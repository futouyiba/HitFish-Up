# HitFish-Up 项目约定

本仓存放 0.3.4.0-B / FCF 的冻结文档与 HTML 产出。以下规则每次会话都适用。

## Notion 需求文档怎么写

- **只写「是什么、约束是什么」**，不写过程。
- **不写状态与流程标记**：`canonical`（指向权威的用法） / `pending` / `Working` / `Open Question` / `待裁` / `待定` / `待确认` / `Owner 裁决 + 日期`。
  - 例外：`Canonical output`、`CanonicalStructureType` 这类**技术术语**要保留。
- **不承载历史推演**：过时内容**迁出到归档页**（标题前缀 `Historical｜`），**不在页内留历史段落、隔断或免责遮罩**。
- **决策留痕归治理页**，不进需求文档正文。
- 判据是这一句：**「这段在说『现在是什么』，还是『旧的是什么』？」** 说现在的 → 改成现行措辞；说旧的 → **整段迁走**，**不要逐条改历史文本里的令牌**（那会造出半新半旧、自相矛盾的段落）。

## 写 Notion 时的两件事

- **不要在标题、粗体或表格单元格里，把四个连续星号当字面内容写进去** —— 它会破坏结构（标题被吞、整节折成一行、表标签丢失）。要指代它请写「四个连续星号」。
- 判断一页有没有被写坏，**看结构**：标题被吞（标题行里混进 `<br>` / `<table>`）、非代码块内的折行、`<table>` 与 `</table>` 不配平、单元格尖括号被转义成标签形、围栏不配平。**不要只看某个字符序列的计数。**
- **可跑检查**：`python3 tools/check_doc_hygiene.py --prefix "<页面前缀>"`。
  - 它**自带自检、且默认先跑自检**；自检断言的是「命中了哪条判据」，不只断言「报了」——否则假证明照样能骗过去。
  - **一页都没匹配到时退出码 2** —— 那是「**没能检查**」，不是「干净」。前缀拼错、页面被改名都会走到这里。

## 给别人的 brief / 任务书

- 里面的**数字必须已落页**，或明确标注「未落页 / 待核」。下游没法分辨一个数字是裁定还是口误。
- **基准变更时，必须回溯修订所有在飞的 brief。**

## 跨会话

- 跨会话建议**不得反向覆盖**某个会话 Owner 的已批决定。冲突时**按已批执行并上报**，不反向覆盖。
- 跨会话消息使用工具返回的准确 session ID，不猜测目标身份。
- 按名称投递出现 `Paused until your user's next message` 或 `No agent named…` 时，若当前环境提供 `mcp__ccd_session_mgmt__send_message`，改用该工具按 `session_id` 投递；不反复重试名称解析。
- `queued` 表示已排队，不重复发送；投递成功不等于对方已读或已执行。
- 若返回权限拒绝、目标不可用或发送限流，按实际错误处理；不把备用通道当作绕过权限或限流的手段。
- 自动消息始终标明来源，不构成人工批准或新增授权。

## 协作路由

- 任务可在当前 agent 权限、职责和已定契约内自行收敛；需要安排、排队、合并或跨线协调时询问主代理。
- 涉及权威不足、多读法并存、可能改变产品语义/范围/归属，或与主代理无法收敛的语义/裁定问题，可直接咨询指定裁定人，同时告知主代理；不因一般不确定性自动升级。
- 各 agent 仅在已有授权内执行写入、派审、合并或对外动作。

## 本仓

- **本仓是公开仓**：不写入内网 URL、Notion 页面 id、凭据。
- 已落成的七页编辑器规范以 Git Markdown 作为 Agent 协作与规范文本的唯一可 diff 源；Notion Current 是面向人和对话式阅读的发布投影。Notion 的格式与结构规则仍适用于后续发布，但发布不在本批执行；完整边界见 [文档权威与发布投影](docs/authority-model.md)。
- 根目录的 `AGENTS.md` 是一份**文档**（独立审核策略），不是 agent 指令文件。
