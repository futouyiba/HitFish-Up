# 落仓说明 —— 《偏移检查报告 0.3.4.0-B（2026-09-21）》

**这份说明由 B 线主代理写，不是报告作者写的。** 报告正文**一字未改**（只有一处**公开仓必须的**删改，见 §5）。
**用途**：让读到这份报告的人**不会把它当成「当前状态」**。

---

## 1. 它的基线，以及「读它的时点」

| 项 | 值 |
|---|---|
| 报告声明的实现基线 | `bae6e6f`（`futou/hitfish-0.3.4.0-b-batch27`，写报告时主检出的 HEAD） |
| 报告里 `file:line` 锚**实际解析**的修订 | **横跨两个修订** —— 一部分只在 `bae6e6f` 上成立，另一部分只在下一笔 `3cb3074` 上成立（差因：`persistence.ts` 在两者之间、**旧行 922 之前**插入了 **＋56 行** ⇒ **>922 的行号全体位移**） |
| 报告之后该分支的进展 | `3cb3074` → `ecbd1df` → `b003dfe`（**其后已有改动落在它判过的点上**，见 §2） |

⇒ ★ **没有任何单一修订能让报告全部锚成立** —— 这不是笔误，是「**行号是易变的锚**」的实测：
同一份报告内的行号会横跨两个修订，而**作者自己看不出来**。
**正解是「每个锚带它自己的 commit」，不是把行号改成另一种写法。**

## 2. 报告之后已被修掉的（**读它时必须知道**）

| 报告里判的 | 现在的状态 |
|---|---|
| **`ADJ-07` 偏移**（注释写默认 `CORE`、无代码折出） | **已修** —— `ecbd1df`（`fix(editor): 两条实现侧偏移 ＋ 补回 #27 漏提交的 app.ts`）之后，`src/editor/policyTemplate.ts:109` **真的是** `return payload.roles[component] ?? 'CORE';`，并配了新测试 |
| **`ADJ-02` 偏移**（只对 `TIME_PERIOD` 实施存在性检查） | **已由 `#27` 落了大半** —— 读者 `persistence.ts:790/794/800/806`、写者 `:949/957/969/978`、求值 `evaluator.ts:160/168/193/215`、校验 `validator.ts:120/127/135` **四组件齐**。**剩余两处**见第 3 节 |
| `ADJ-11` / `ADJ-09` | **仍成立**（`refs` 零写者；`src/` 里 `staged` **0 命中**） |

## 3. 报告之后新查出的（**报告里没有的三处漏网**）

1. **`src/core/authoring.ts:337`** —— 它 `:324` 自称「与 validator 的 `validateActiveProfile` 同规则」，**而那条规则今天覆盖四组件、这处只覆盖一个** ⇒ **注释已成假陈述**；后果是「validator 抛、诊断面板静默」。
2. **`src/data/persistence.ts:902`** —— 它让**时段**能自建档案、另三个缺档案直接抛 ⇒ **那是「时段特权」，与 `ADJ-02` 逐字「缺席合法性只由 `Role` 决定；时段不特权」相抵**。★ **裁定（执行 `ADJ-02`，非新裁定）**：判据从 `f === 'TIME_PERIOD'` 改成它真正描述的那件事 ——「**非同构源**」（`resolveComponent:830–831` 的「水温/时段迁移期」／`itemsOfRow:720–721` 把水温与时段并列）。
3. **`src/data/persistence.ts:1144`** —— 注释复述一个**已废的返回值契约**（说 `namedItemsOfProfiles` 对无档案「返回全 0」，而 `:800` 今天返回 `null`）。**行为惰性** ⇒ 功能无害、**认知有害**。

## 4. ★ 报告 §11.4 里那处锚的更正 —— **错的是主代理（我），不是报告作者**

报告 §11.4 现在把 `858–885` 标为「原写法」、把 `870–891` 标为「更正」。
**真相是两边都对，缺的是 commit：**

- `bae6e6f`（**报告声明的基线**）上：注释块 **858–863**、函数体 **`onApplyPeriodPreset` 864–885** ⇒ **`858–885` 成立**；
- `b003dfe`（当前头）上：注释 864–869、函数体 **870–891** ⇒ **`870–891` 成立**；
- **`onApplyPeriodPreset` 全历史从没有一版起始于 858**；「`858` 是 `onRoleEdit`」**在 `b003dfe` 为真、在 `bae6e6f` 为假**；位移因 `ecbd1df` 对 `app.ts` **+18/−4**。

⇒ ★ **我曾在 HEAD 上核一份声明在 `bae6e6f` 的报告，据此判它「锚错了」并把更正给了它 —— 那是我的错**：
**核 `file:line` 之前，第一步是核「我读的修订 ＝ 文档声明的基线」。**
⇒ **这是同一条失效模式的第三次命中，且第一次发生在两个复核者之间。判据：行号锚不一致时先问「你锚在哪个 commit」；「改回」是最坏的修法 —— 它把「两把尺子」变成一次真实的引入错误。**

## 5. ★ 那处 `FishEnvAffinityRef` 的定性 —— 报告作者的更正推翻了主代理

报告 §2.5 的定性是「**未决 ＋ 第二层没有落点**」，**不是**主代理先前给的「**一个词兼了两个寄存器**（两义）」。依据：`ownerOf` 的**两个分支都产不出 `FishEnvAffinityRef`**（一个给 `row:mature`＝桶名、一个给 `species:<id>/<scope>`），且**它是个没接线的模块**（`ownerKeyOf` 生产调用者 0）⇒ **没有任何使用点可供消歧** ⇒ 「在使用点消歧」这个修法**前提就不成立**。

★ **裁定已到（权威源＝记录页 §342，`AR-FCF-CT-01…04`；本仓投影载体＝审阅包 `contract-cards.md`）**：Role 的第二层落点 ＝ **`AffinityRolePatch`，key `(row_key, component)`**；numeric override 与 Role override **故意不同粒度**（bucket-level／row-level），**不得为统一 record shape 合并**。⇒ **第二层有已裁的落点，只是没接线**；形状变更**有据了**。

## 6. 我在落仓时做的唯一改动（**公开仓红线**）

本仓是**公开仓**。报告正文里有一处**绝对本地路径**（第 15 行「主检出 `/Volumes/…/preCompute` 正 checkout 在它上面」），已改成相对描述「主检出（本地 `preCompute` 仓）正 checkout 在它上面」。
**除这一处外，报告正文逐字未动。** 全文已复核：**无内网 URL、无 Notion 页面 id、无其它绝对路径**。

## 7. 仍未同步

报告 **§11.4 的两处锚写法**与**时点锚**由**报告作者**修订（本说明 §4 已先行给出正解与归因）；作者修订版到达后**另提一笔**，本说明不替代它。
