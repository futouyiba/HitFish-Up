# 修订 2｜对第一轮全部 findings 的逐条处置

上一轮复审打在 `c6e4346`。本文件覆盖**全部 15 条**：`A-*` 9 条（材料侧第一份报告）／`B-*` 2 条（材料侧第二份）／`FIG-*` 4 条（Figma 侧）。

**编号说明**：不同报告的 `F` 编号**不是同一套**（上一轮本侧曾把一份报告的 `F-01` 当成对另一份的回应）。本文件起**一律带报告前缀**。

---

## 一、`A-*`（9 条）

| ID | 严重度 | 处置 | 落在哪 |
|---|---|---|---|
| `A-F-01` | BLOCKER | **已改**（在 `REVISION 1`）：**撤掉「由外部认证忠实性」这个要求**，收窄为「**沿革残留**」—— 只判**可判的那一半**（正文里有没有段落在叙述它自己的沿革）。**理由**：两份原始输入不在本目录、结构上不可判；原材料含已被推翻的内容，按规不进公开材料。 | `REVIEW-PROMPT.md` §一.1／§二.1 |
| `A-F-02` | MAJOR | **已改**（在 `REVISION 1`）：Prompt 的「§17 那 **8** 条」→「**7** 条」，并注明同类相抵另有一条在 `§18.8`。 | `REVIEW-PROMPT.md` §二.2 |
| `A-F-03` | MAJOR | **已裁已改**：卡2 那行把「跟随／**恢复**」并列成"删一条"的一侧 ⇒ **错**。真机制**不是"两个操作打架"**，是**那一行把「恢复」这个已退役的模糊词当操作名用了**（它在同卡另两处只以「**不得合并成一个「恢复」**」的形式合法出现）。**改为**「**`ADD`／`SET`／`CLEAR` 各落一条；只有「跟随」那一支删条**（`INHERIT`／桶层缺省 `absent` ⇒ 不造记录）」，并补上 §4 的逐字（**`CLEAR` 计入本层操作数**）。 | `contract-cards.md` 卡2 |
| `A-F-04` | MAJOR | **已裁已改**：RoleControl 补记原写「`CLEAR` ⇒ **回到继承**」⇒ **错**（**这条是撰写者的错**）。依据《编辑器持久层契约》§3.4 逐字：**`CLEAR` ＝ 移除继承自物种层的操作、回到物种当前 Policy Template 的 raw 值**；**「回到继承」是桶层缺省（`absent`）那一支**；**两者是两个不同动作、UI 必须区分**。独立旁证：§4 的用户语言里 `CLEAR` ＝「仅使用当前来源」。 | `contract-cards.md` 补记 |
| `A-F-05` | MAJOR | **已裁已改**：卡1 原写「`SELECT_SOURCE` / `FOLLOW_PARENT` **写持久**」⇒ 会被实现成**下拉一选即写盘**。**改为**：`SELECT_SOURCE` 先形成 **candidate（不写持久）** → **Rebase Preview** → **显式确认** → **原子提交**；`FOLLOW_PARENT` 在**解除／切换**那一步才写。依据汇编 §8 逐字「换 Source 是高影响动作：候选来源 → before/after Resolve → Rebase Preview → 显式确认 → 原子提交」。 | `contract-cards.md` 卡1 |
| `A-F-06` | MAJOR | **已裁已改**：卡10（模板**完整值**编辑）原写「档位随 `op` 走：`ADD` 不带 Tier、`SET` 才用 Tier」⇒ **搬错了寄存器** —— 那条规则的依据（记录页 §199 ⑥①）讲的是**值记录**的 `{op, value, tier}` 形状；而**模板是完整值资产，模板里没有 op**（§14 逐字：operation 只存在于物种 Recipe 与桶 patch 上）。**该行已删**，并明写「**本卡不出现 `ADD`／`SET`／`CLEAR` 这类 operation 语义**」。**卡2 里同名两行保留**（那才是它的寄存器）。 | `contract-cards.md` 卡10 |
| `A-F-07` | MAJOR | **已裁已改**：一文件不能同时宣称两种状态。**两批都已冻结**（批次① 卡1–卡7 依记录页 §165；批次② 卡8–卡12 依 §244）⇒ 文件头**写明两批各自状态**；Part 3 标题**去掉「起草稿」**；收口提示 #1 改成**一条明确的挂项**（卡8 那半句待实现线逐条对表），并明写「**这是本批唯一挂项、不代表整批待确认**」。 | `contract-cards.md` 头部／Part 3 |
| `A-F-08` | MAJOR | **已裁已改**：汇编 §8「可选来源矩阵：Temperature ＝ `SHARED_TEMPLATE \| SPECIES_CONCRETE`」**层次无关**，会被读成**桶层也列 Concrete**。依据《编辑器与 Resolve》§11.3 逐字「桶层另有「跟随物种」选项，**不把任何物种的 Concrete 当通用可选项**」（**「任何物种」含当前物种**）⇒ **按层分写**：**前层（物种层）** 温度含 `SPECIES_CONCRETE`；**桶（覆盖层）** 一律只有 `SHARED_TEMPLATE` ＋「跟随物种」。 | `component-contract-consolidated.md` §8 |
| `A-F-09` | MAJOR | **已裁已改**：卡片标题用了页面上不存在的名字，而 §19 把它们列为「待命名」—— **两者不冲突，但射程要写死**。**卡的 `Component:` 行是「卡片层标识」**（允许与页面语汇不同形），**不得拿去建代码枚举／schema 字段／选择器／i18n key**；**页面上已有名字的物一律以页面名为准**（草稿 `FieldValueRow` ⇒ **`FieldValueControl`**）。已在卡片文件里**显式写下这条命名口径**（此前只存在于对两个名字的个案处理里）。 | `contract-cards.md` 头部 |

---

## 二、`B-*`（2 条）

| ID | 处置 |
|---|---|
| `B-F-01` | **真缺陷 —— 而且是本侧在合并两份草稿时引入的。** §3 里「在这一层没有操作（`INHERIT` / patch 缺省）时**直接用来源值**」把**物种层的 `INHERIT`** 与**桶层的 `patch 缺省`** 混成了一个"缺省"。**页面是清白的一侧**（《编辑器与 Resolve》§11.1 逐字「`absent` ＝ 跟随物种层 operation」／§11.2 逐字两者是**两个不同动作**）。**已裁已改**：该行**拆成按层两支**，并明写「**`absent` 不是「用来源值」**」。**全页交叉引用已扫**：该行是**唯一**走偏的一处，改它没有造出新矛盾。 |
| `B-F-02` | **与 `A-F-04` 是同一处**（两条独立审阅各自抓到）⇒ **已裁已改**，见上表 `A-F-04`。 |

---

## 三、`FIG-*`（4 条）

| ID | 处置 |
|---|---|
| `FIG-01` | **已裁**：`108:311` 四张卡内没有 Source 变更入口，与 §7「组件卡是摘要 ＋ 快速编辑入口，可就地改 Source／Template 与 Role」相抵。**独立复核过事实面**：顶部 `templateRow`（`108:314`）确并排四个来源选择器，`componentCards`（`108:335`）四张卡内确无入口。⇒ **独立顶部 `templateRow` 与卡内只读 Source 不能继续并存为同一 Current 形态。** ⚠️ **改图在执行中（另一个会话），不在本 head 内** —— 下轮带图。 |
| `FIG-02` | **已裁**：卡与 Policy 只投 `CORE`／`SECONDARY`／`IGNORED` badge，**表达不了 `absent` vs `CLEAR` vs `SET`**。**改法依据已出**（＝上表 `A-F-04`：两者是两个不同动作、UI 必须区分；Role 双入口单 Truth、**granularity row-level**）。⚠️ **改图在执行中，不在本 head 内。** |
| `FIG-03` | **已裁**：`230:673` 展开态 provenance 逐字「共享模板 `0.60` →（物种 `ADD` `−0.20`）`0.40` →（本层 `CLEAR`）`0.60`」把**已被 `CLEAR` 替代的上级 `ADD` 画成了连续计算链的一环**，与 §4「**不得画成连续数学流水线**（整层替换、不是叠加）」相抵。⚠️ **改图在执行中，不在本 head 内。** |
| `FIG-04` | **已改**（在 `REVISION 1`）：`figma-current.md` 的「**26 格**」→「**12 行双列 ＋ 1 行单列 ＝ 25 个 `FieldValueControl`**」，并加一句「末行只有一列、不要按 13 × 2 算成 26」。 |

---

## 四、本侧据此改了自己的检查（`REVIEW-PROMPT.md` §二.7 新增）

`B-F-01` 与 `A-F-08` **是同一个形态**：**把"按层分写"的规定压成了一句层次无关的话** —— 读起来完整，**但每一层都被错配一次**。这是本目录这类材料**最主要的失效形态**，而它此前**不在审查项的射程内**。

⇒ 已加为**审查项 7「压层」**：**凡提到 来源／操作／缺省／picker／矩阵 的行，若整句里没有出现层名，就标出来。**

⇒ 同时补一条**本侧自己的检查**（外部审阅方结构上做不了，因为权威页不在本目录）：**合并产物 ↔ 权威页 对拍** —— **合并这个动作本身会造错，它需要自己的检查。**

---

## 五、通过判据 → 见 `REVIEW-PROMPT.md` §六（两层门）

**开发门 ＝ ≥1 位在 exact head 上通过**（不等合并）；**合并门 ＝ 全部通过 ＋ 连续 2 小时无新意见**。`REVISION-1.md` 第四节写的旧判据已被取代。
