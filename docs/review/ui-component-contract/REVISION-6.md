# REVISION 6 — 第三轮：材料侧 9 条 ＋ 图级的**物证缺陷**（2026-09-21）

对齐 head：`f14b6a2e882ca97c505d8739dacf71254093b638`。本轮我们**先认一条自己的缺陷**（第零节），再逐条处置。

---

## 零、你们指出的那半：**我们的物证是伪的** —— 认，且比你们说的更硬

**你们说**：`REVISION-5.md` **声称**已把「Role 入口」改为「水温 · Temperature 的 Role」并新增「目标行…」，但**本 head 里三张 PNG 与上轮逐字节相同**、`108:311` 上 Policy 底部**仍是旧文案** ⇒「所述改图在本 head 无可核验证据」。

**我们复现并加硬了它**：

| 事实 | 值 |
|---|---|
| 三张 PNG 何时入包 | `e44e91c`（**REVISION 3**），此后**三轮一字未动** |
| 导出时刻 | `2026-09-21T00:40:28Z` |
| 画布末次写入 | `2026-09-21T01:20:36Z` ⇒ **图早于画布 40 分钟** |

**三张重导后逐像素比对 —— 分两档，我们只认其中一档**：

| 图 | 字节 | 差异落点 | 判定 |
|---|---|---|---|
| `108:311` | 171330 → **174828** | `y=442–455`、`x=331–1123` —— **恰好一行 chip 的高度** | **真内容差异** |
| `176:283` | 13285 → **13485** | 改动像素 **64**，**全在四角行**（每行 2–16 px） | **圆角抗锯齿**，非内容 |
| `230:673` | 24055 → **24410** | 改动像素 **100**，**全在四角行** | **圆角抗锯齿**，非内容 |

⇒ **「字节变了」不等于「内容变了」** —— 后两张我们不声称"变了"（这一条也写进 `figma-current.md` §六）。

**那条带裁出来逐字对照（决定性）**：

```
旧（包内）  Role 入口 | 层级 · 物种层 · 默认（各行继承） | 桶层操作 ▾ | absent 跟随继承 | CLEAR 回到模板 raw 值 | SET 设置            ← 6 枚
新（本 head）水温 · Temperature 的 Role | 层级 · … | 桶层操作 ▾ | absent 跟随继承 | CLEAR 回到模板 raw 值 | SET 设置 | 目标行：LAKE_A × LARGEMOUTH_BASS/Q3   ← 7 枚
```

⇒ **包里那张图不只是「没有证据」，它正面显示的是改动前的状态**。定性：**「声称」为真、「物证」为伪 —— 缺陷在我们的交付物**（`REVISION 5` 声称改图、却带着 40 分钟前的图）。

**本版做两件事**：① 换三张新图；② 附**节点级回读**（不依赖图像通路），让「图更新了没有」与「图里画了什么」**各自可被独立判**。

---

## 一、换图 ＋ 节点级回读（本版随包物证）

| 文件 | node | 像素 | 字节 before → after | md5（新） |
|---|---|---|---|---|
| `img/0.3.4.0-B-editor-structure-108-311.png` | `108:311` | 1680×1220 | 171330 → **174828** | `66f93fe2d2bba209eb25772d4583d33e` |
| `img/0.3.4.0-B-policy-176-283.png` | `176:283` | 820×92 | 13285 → **13485** | `0f269994fff541dbc493ad83aaa5c267` |
| `img/0.3.4.0-B-card2-state10-230-673.png` | `230:673` | 890×150 | 24055 → **24410** | `62ac2958b3b6a4c0775e89b3b69dae8b` |

**节点级回读**（`FCF-0.3.4.0-MockUp` ／ `fdlmpZPM7Onh7hKFpoaUBL` ／ `108:364` ＝ `SpatialOpportunityPolicyBlock`，854×122）：

| chip | 文本 |
|---|---|
| `242:670` / `242:669` | **`水温 · Temperature 的 Role`** |
| `242:672` / `242:671` | `层级 ▾ 物种层 · 默认（各行继承）` |
| `242:674` / `242:673` | `桶层操作 ▾` |
| `242:676` / `242:675` | `absent 跟随继承` |
| `242:678` / `242:677` | `CLEAR 回到模板 raw 值` |
| `242:680` / `242:679` | `SET 设置` |
| **`252:662` / `252:661`** | **`目标行：LAKE_A × LARGEMOUTH_BASS/Q3`** |

**几何**：chip 行 `y=107`、高 `14` ⇒ 内容到 `y=121`，容器高 `122`；最右一枚 `617+199 = 816 ≤ 854` ✓。

---

## 二、`FIG-02` **剩余那半**（层级歧义）—— 我们**不闭合它**，并说明它与 `R3-F-05` 是同一个根

**你们的新定性我们接受**：不再是「无物证」，而是 **`242:671`「层级 ▾ 物种层 · 默认（各行继承）」与 `242:673`「桶层操作 ▾」并列、未分离成不同编辑上下文** ⇒ 不能确定点 `CLEAR` 改的是**物种默认**还是**该行覆盖**。你们明说不要求某种固定布局 —— 我们据此**不重画**，先解决下面这个根。

**根（`R3-F-05` ＋ `FIG-02` 剩余半是同一件事）**：**species 层 default 与 row 层 override 的分层没有被表达** ——
- **卡侧**（`R3-F-05`）：`RoleControl` 的 durable key shape 只给 `species_key + component + scope_key(row_key) + role`，**只能表达 row-level**，**未给物种层 `INHERIT/SET` default 的持久化形状** ⇒ 会迫使实现自行发明「默认怎么存」。
- **图侧**：同一件事表现为「分不清 CLEAR 改哪层」。

⇒ **一处改、两处收**。**这动的是持久化键的形状（契约级），我们已把它提请裁决，不在本版自行定形。**

---

## 三、`R3-FIG-01` —— **相抵成立，我们认，且不接受用 annotation 代替契约文本**

你们引 `§7`：「卡的 Source 下拉与**焦点编辑栏**的 Source 选择器指向同一个组件 Recipe Source binding」。**我们逐字核过，`§7` 确是这么写的**；而图上实际是**顶部 `templateRow 108:314` ＋ 卡内入口 `108:335`**，`108:495`（`Contextual Editor · Structure`）**没有** Source 选择器。

⇒ **你们说得对：不能用 annotation 把另一种入口拓扑解释成已满足 `§7`。** 我们先前在 `figma-current.md` §七做的正是这件事 —— **那条解释在没有改契约文本之前不成立**。

⇒ **两个方向，我们不在本版替它选**：
- **(甲) `§7` 为现行** ⇒ 图要为焦点编辑栏补 Source 选择器（或删顶部行的"第二入口"身份）；
- **(乙) 顶部行入口为现行**（依据：《编辑器持久层契约》§8 首行「每组件一个**前层** Source 选择器」＋ 一条 Owner 裁定「与前层同层／不要求先进入组件展开区才能选／显眼便利」）⇒ **先改 `§7` 的文本**，annotation 的解释才成立。

**顺序不能反：先改契约，再谈图。** 契约正文的笔不在本席 ⇒ 已提请。

---

## 四、逐条处置（材料侧 9 条）

### `R3-F-01` 卡1 Durable mutation 相抵 —— **已改**
`Actions` 已要求 `FOLLOW_PARENT` 走「candidate → Rebase Preview → 显式确认 → 原子提交」，而 `Durable mutation` 仍写「**解除／切换那一步才写持久**」⇒ 留下**「解除即写盘」的第二口径**。
**改法**：把该句改成 **「`FOLLOW_PARENT` 的持久变更 ＝ 删除 `sourceOverride`，同样发生在该动作的『原子提交』那一步 —— 它与 `SELECT_SOURCE` 是同一条边界，不得读成『解除即写盘』」**。

### `R3-F-02` 卡2 四格缺口 —— **已改**
`桶层 × 数值` 与 `桶层 × 枚举` 两格只写了「按实际继承到的那个操作给串」，**漏掉「物种上层无操作」时的桶层缺省支**，与同卡 `Must show` 互相补丁。
**改法**：两格各补 **「物种层无操作时该缺省支显示『仅使用来源』」** ⇒ 四格自此自足，不再依赖 `Must show` 打补丁。

### `R3-F-03` 卡3 `Must not` 射程过宽 —— **已改**
原文「不因 UI 统一强行给**枚举/边界参数**上 ADD」**把合法的数值边界参数一起禁了** —— `acceptMin / favMin / favMax / acceptMax` **本身就是那 5 个数值项里的边界参数**。
**改法**：**「射程只落在枚举绝对值项 `falloff_shape`；`acceptMin / favMin / favMax / acceptMax` 属那 5 个数值项、`ADD` 合法，不得一并禁掉」**。

### `R3-F-04` 汇编 §12 相抵 —— **已改**
同节先写「5 数值项 ＋ `falloff_shape` 枚举」，随后仍写「**6 个数值参数**」⇒ 汇编把 enum 压回 numeric。
**改法**：改为 **「6 项参数（5 数值 ＋ `falloff_shape` 枚举）」**。

### `R3-F-05` RoleControl durable key shape —— **本版不处置，见第二节**
它与 `FIG-02` 剩余半同根，动的是持久化键形状 ⇒ 已提请裁决。

### `R3-F-06` 卡8 导入目标物种 —— **已改**
该动作更新 `SPECIES_CONCRETE` 的 identity ＝ `(speciesId, componentType)`，**而卡8 所在的独立 Template Workspace（`22:2`）Reads 里没有 current species、也没有 species selector**。
**改法**：**入口必须显式选目标物种**（有当前物种时可默认为它），**不得用隐式/未知物种执行**；**未选物种 ＝ 该动作不可执行**，不是「就用当前物种」。**等价替代**：把该入口限定到**已有明确 species context** 处。（你们给的两个方向我们都接受，取了第一个、并写明第二个等价。）

### `R3-F-07` 批次级状态相抵 —— **已改**
卡2 降级只改了文件头，**`Part 2` 标题仍写「七张 · 已冻结 v1.1」、文件头又写「两批都已冻结」** ⇒ 一个实现者看到两个相反状态。
**改法**：**①** 文件头加 **「凡『批次』级状态一律排除 `A①-卡2`（即：批次①可作无条件执行依据的是六张）」**；**②** `Part 2` 标题改为 **「六张已冻结 v1.1 ＝ 记录页 §165；`A①-卡2` 见文件头，暂不作为无条件执行依据」**。
（这一条命中我们自己的规矩：**「已修局部」必须与「待重估大头」并列** —— 先前只修了文件头。）

### `R3-F-08` 汇编沿革残留 —— **已改（并附一处判断，供你们复核）**
你们引 `Review Prompt §2.1`（Current 正文不叙述自身沿革），指出 `§3/§4/§8` 仍留「本节自记录页 §270 更正 —— 原写法…」「本行自记录页 §292 更正 —— 原句…」「本节自记录页 §271 更正 —— 原写法…」等句。
**改法**：**全部移除沿革叙述，只留现值 ＋ 出处**（共 5 处：`§3` 两处、`§4` 三处、`§8` 一处 —— 按改动行计）。改后回读：`自记录页` 0 命中、`更正` 0 命中、`原写` 0 命中、`原句` 0 命中。
**一处判断请你们复核**：改后仍余 `草稿` 字样 **13 处**，**全在 `§19`（命名裁定节）与两条「本轮裁定 g」的出处引用里**。我们判**那是「出处引用」、不是「沿革叙述」**（它们不叙述"本条的旧说法是什么"，而是指认某个名字只在草稿里存在、并给出裁定出处）。**若你们判它也算沿革，我们再清。**

### `R3-FIG-01` —— **相抵成立**，见第三节（本版不闭合，已提请）

---

## 五、本版的边界

- **本版只改文本 ＋ 换图**，**不改任何契约语义**（除四节里逐条列出的那几处措辞/射程修正）。
- **§二、§三两条不在本版闭合** —— 它们需要契约级裁决，已提请 ⇒ 请你们在下一轮按"仍未闭合"计，不要把本版读成对它们的回应。
- 图侧的**几何与节点数**：本版**未新增/删除任何节点**（换图不涉写入）。
