# REVISION 9 — 契约正文已落 ＋ 图侧 `R4-FIG-02` 已落（2026-09-21）

对齐 head：`78a7a85`（REVISION 8）。

本版把三件事同步到现态：**①** `R4-F-01` 的**契约正文已落**（REVISION 7 自标的「落了一半」到此关掉）；**②** 图侧 `R4-FIG-02` 已落；**③** §7 一句按产品口径收紧。

---

## 一、`R4-F-01` 契约正文已落（《编辑器持久层契约》）

**五节**：`§3.1`／`§3.4`／`§4.2`／`§1.1`／`§2`。页 `34111 → 36404`（两次写）。落的是**裁决后的形状**（见 REVISION 8 第二节）：

- **§3.1** 标题改 **`Species Base Record｜物种底板记录（每物种一条）`**；新增 `policy_source_binding` 与 `fail_env_coeff` 行；`roles` 行改成「四个组件的 Role op」（`SET` 携带值／`INHERIT` ＝ 无记录）；
- **§3.4** 改成**两条 row-level 轨道**（`AffinityRolePatch` ＋ `AffinityFailEnvCoeffPatch`）；`scope_key` → **`row_key`**；`species_key` 标**属性**；新增 `op` 行与第二张表；**唯一约束 → `(row_key, component)`**；
- **§4.2** 的「覆盖项」并集行按记录拆开；**§1.1** 的交叉引用按四名分层改对；**§2** 词汇与 §3.1 逐字对齐。

**供你们独立核（指纹 ＋ 取法）**：

| 节 | 字符数 | `sha256[:24]` |
|---|---|---|
| §3.1 | 1887 | `6b5ec2e0cc6d0379a2e6a472` |
| §3.4 | 2742 | `f6276c4da0e1d93548fa2316` |
| §4.2 | 635 | `792e6570c8aab30feb216880` |

**⚠️ 取法必须照这句来，否则会算错**：**行首全层级锚** —— 用 `(?m)^#{1,6} ` 匹配标题行，取**含本节标题行、不含下一节标题行**的切片。
（我们先前在本族内互相核对时栽过一次：裸 `find('## 3.1')` 会命中 **`### 3.1` 里的那四个字符** ⇒ 两端各偏一位 ⇒ **同长度、异哈希**，于是两边"一致地错"。**所以指纹必须连取法一起给。**）

---

## 二、图侧 `R4-FIG-02` 已落

**改的是默认态不是重画**：Policy 控制条（`108:364`，854×122）分成**两组 ＋ 两条分隔线**：

```
[水温 · Temperature 的 Role] │ [物种层 · 默认（各行继承）] │ [本行覆盖] [目标行：LAKE_A × LARGEMOUTH_BASS/Q3] [absent 跟随继承] [CLEAR 回到模板 raw 值] [SET 设置]
```

- `242:672`：`层级 ▾ 物种层 · 默认（各行继承）` → **`物种层 · 默认（各行继承）`**（中性灰 `#edf0f5`）；
- `242:674`：`桶层操作 ▾` → **`本行覆盖`**（角色蓝 `#e3edfb` ＋ `#2e63b8`）；
- 新增两条分隔线 `266:661`(148,107)／`266:662`(279,107)，各 1×14。

⇒ **判读靠结构、不靠读文字**：**`CLEAR` 物理上位于「本行覆盖」组内** ⇒ 点它改的是本行覆盖，不是物种默认。

**回读**：页节点 `2311 → 2313`（Δ=+2 恰为两条分隔线）｜`108:364` kids `8 → 10`｜右缘 **797 ≤ 854**｜条内两两相交 **0 对**｜转义 `amp 0`｜**未引入新色**（两色都是本文件既有的）｜**母件一个没动**｜容器高度未改（遵守已登记的「高度未改是有意的」）｜`ann[0]` 零写入。

**⚠️ 一条已知的表征缺口，我们主动说**：`层级 ▾` 与 `桶层操作 ▾` 两个**控件记号**被去掉了（它们现在是**组头**）。就本轮要求我们接受（两个作用域同时可见 ⇒ 层选择器冗余），但后果是 —— **物种那一组不列操作 ⇒ 图上没有表示「物种层 Role op（`INHERIT / SET`）怎么改」**。
⇒ **本轮不补**（复审没要它；补了会把控制条撑到 ~820）。**若你们认为该补，请提出来** —— 那时我们需要先定**记号形制**（本族禁发明新记号），再动手。

---

## 三、§7 一句收紧（按产品口径）

`§7` 第三条原把卡的入口叫「Source 下拉」，**把 `Template` 漏在名字外**。已改成「**组件卡的 Source / Template 下拉**（卡上这一个入口同时覆盖 Template 与 Source —— 换模板就是换 Source 绑定）」，并补两句：**卡上这两样都是就地可改 —— 卡不是只读卡**；**两者都不是卡自己存一份副本**。

⇒ 与同节第一条「组件卡是摘要 ＋ 快速编辑入口……可就地改 `Source / Template` 与 `Role 三态`」不会再被分开读。**焦点编辑栏那条「不提供 Source 下拉」说的是右栏、不是卡**，两者不冲突。

---

## 四、仍挂着的一处（非本版能闭）

**一处字符级取舍**：裁决文本里 `CT-01` 的 `AffinityRolePatch` 字段表**没有 `species_key`**，`CT-04` 的 canonical shape **有（作属性）**。我们**取 `CT-04`**（更具体，且其正文写明「`species_key` 继续留作组织／查询／reconcile／诊断」），**并标明这是我们的取舍、不是裁决方明说的** —— 若你们或裁决方澄清，按澄清改。

---

**通过判据**见 `REVIEW-PROMPT.md` §六（两层门）。
