# 修订 1｜对第一轮 findings 的逐条处置

上一轮审阅打在 `e71d250d8f5d`。**本文件的用途：让复审不必回头比 diff** —— 逐条说明「改了 / 没改、为什么」。

---

## 一、本轮已改（3 处，都是**包自身的缺陷**，不是契约的错）

| finding | 改在哪 | 改了什么 |
|---|---|---|
| `F-01`（BLOCKER｜材料侧） | `REVIEW-PROMPT.md` §一.1 ＋ §二.1 | **撤掉「由外部认证忠实性」这个要求。** ① §一.1 点名那两份原始输入处，补一句「**这两份都不在本目录、也不在后文任何位置**」；② 原题「忠实性」**收窄为「沿革残留」** —— 只判**可判的那一半**（正文里有没有段落在叙述它自己的沿革，形如「原来…后来改成…」「此前的错误收敛」），并明写**不要去核与原始输入是否一致**。**理由**：原材料**含已被推翻的内容**，按项目的对外规则不进公开材料；忠实性改由内部背书。 |
| `F-02`（材料侧） | `REVIEW-PROMPT.md` §二.2 | 「§17 那 **8** 条」→「**7** 条」，并补一句**同类相抵另有一条单列在 §18.8**。⇒ **这是我给的数与实物不符**（§17 实际只有 1–7）。 |
| `FIG-04`（Figma 侧） | `figma-current.md` §一「Structure 的表示」 | 「**26 格**」→「**12 行双列 ＋ 1 行单列 ＝ 25 个 `FieldValueControl`**」，并加一句「末行只有一列、**不要按 13 × 2 算成 26**」。**成因**：我用**乘法代替点数**。 |

> **另有一处同形，是本次改文件时自己撞见的**：`README.md` 原文写「**四份文件**」而表里列了**五**行。已改为「六份文件」（含本文件）。**「8 条／26 格／四份」是同一个病** —— 我报的数与实物不符。⇒ **已立的纪律：对外材料里凡出现计数，落笔前回读实物数一次。**

---

## 二、本轮**没动**的，与为什么（**有意不动，不是漏了**）

下面这些都是**真 finding**，但**不是改字能消的** —— 它们要**先裁**（定哪一侧是现行，然后两处一起改）。裁完会以**修订 2** 送回。

- **汇编与冻结卡互相打架（4 条）**：`F-03`（卡2 durable：`CLEAR` 是否持久）／`F-04` ＋ 另一份的 `F-02`（RoleControl 的 `CLEAR` —— 「回到继承」还是「回到 Policy Source 原值」？）／`F-06`（卡10「档位随 op 走」vs §14「模板是完整值资产」）／`F-07`（卡文件头「已冻结 v1.1」vs Part 3「起草稿…待确认措辞」）。
  - 注：**`F-04` 被两条独立的审阅各自抓到** —— 该处可信度高，会优先裁。
- **要补口径（3 条）**：`F-05`（换源的 commit 边界：候选 → 显式确认 → 原子提交，vs 卡1「`SELECT_SOURCE` 写持久」）／`F-08`（Temperature 覆盖层能否**显式 pin** `SPECIES_CONCRETE`）／`F-09`（§19 把 16 个组件名列为「待命名」而执行卡已用它们作标题）。
- **要改图（3 条）**：`FIG-01`（`108:311` 四张卡内没有 Source 变更入口）／`FIG-02`（Card 与 Policy 表达不了 absent vs `CLEAR` vs `SET`）／`FIG-03`（`230:673` 的 provenance 画成了连续算式）。
  - 其中 `FIG-01`／`FIG-02` 的**改法取决于上面 `F-04` 的裁定** ⇒ 会排在它后面。

---

## 三、关于 Figma 半份的可见性（第一轮留下的矛盾）

第一轮两份报告对「能不能看到那个 Figma 文件」说法相反：一份逐字「**Figma 侧：看不到文件**」，另一份却引了图上的 `FR-007 RESOLUTION` 与 `108:311` 的实际构成。

**本侧已核的事**：**文件本身可读** —— 本次即由节点读回数出 `25`（见上表 `FIG-04` 一栏）。⇒ **请这一半按「能读到文件」走。若你仍然看不到，明确说一句**，我们改把关键帧导成 PNG 放进本目录（**图片是内容、不是链接**，不违「内部链接不进仓」）。

---

## 四、通过判据（请照这个给结论）

- **一轮一条回复**，**逐条对应**：本轮请给 —— `F-01`／`F-02`／`FIG-04` **是否接受已改**；其余各条**确认仍是 finding、等修订 2**（若你认为其中某条**本轮就该改**、不必等裁定，请直说）。
- **结论打在 exact head 上**（本包下一版的 commit sha）。
- **通过 ＝ 由你在评论里逐字给出通过。** **「没说话」不算通过**，也不由本侧推断。
