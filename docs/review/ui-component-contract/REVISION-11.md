# REVISION 11 — `FIG-PACKET-01`：随包图已按现画布重导（2026-09-21）

对齐 head：`2924313`（REVISION 10）。

## 你们指出的那条，我们复现了

> 「本 head 的编辑器 PNG 仍为 **174828** 字节，与上轮逐字节相同…亲看图中 Policy 下缘仍有『层级 ▾ 物种层 · 默认（各行继承）』及『桶层操作 ▾』，**没有实时画布新增的分组／分隔线**」

**复现**：现画布重导 `108:311` ＝ **175020** 字节（`sha256[:12] b4602d8fd1a2`），而包里那张是 **174828**（`3a263dabdf62`）⇒ **包里那张确实是旧的**。**这一条成立，不是误报。**

**本版只换这一张**（`img/0.3.4.0-B-editor-structure-108-311.png`：174828 → **175020**）。

## 另外两张：**与包里逐字节相同，故不重导**

按你们说的「不要求重导未变化的另外两张图」—— 我重导后核对，**它们确实没变**：

| 文件 | 重导后 | 与包里 |
|---|---|---|
| `img/0.3.4.0-B-policy-176-283.png` | 13485 | **逐字节相同** ✓ |
| `img/0.3.4.0-B-card2-state10-230-673.png` | 24410 | **逐字节相同** ✓ |

（这也印证了本轮画布改动**只落在 `108:311` 之内**。）

## ⚠️ 同一类缺陷第三次：根因不是疏忽，是**没有连线**

你们的判断我们接受：**REV 3**（随包图早于画布 40 分钟）→ **REV 6**（换图、你们接受）→ **REV 9**（改了画布、**又没换图**）。
⇒ **「改画布」与「重导随包图」之间没有任何机制连着** —— 所以它会第三次发生。**我们正在把它挂到动作必经之处**（写画布那一步的清单／skill），而不是再加一条备忘。

## 边界

本版**只换这一张图**；文字部分由 REVISION 10 承担（§七 已改成镜像 Current 规则、卡 2 已按「落到页后即恢复」解降）。

**通过判据**见 `REVIEW-PROMPT.md` §六（两层门）。
