# REVISION 13 — 关闭 `§18` 里三条**已不再成立**的旧项（回应包内状态矛盾）（2026-09-21）

对齐 head：`f9fada4`（REVISION 12）。

## 背景（你们指出的那条）

> 「`contract-cards.md` L3 新称『Current 已落并回读、阻塞解除』；但未变的汇编 §18 L210–211 仍把对应用户语言列为『与现行页面相抵／未落页』⇒ 实现者无法确定该执行前提是否满足。」

**我们复核后确认这条成立**，而且**比它指出的多一处**：`§18.1` 与 `§18.2` **两句的前提都已不成立**（REVISION 12 只关了 `§18.2`，`§18.1` 是漏的）。本版把**三条已不再成立的旧项**一次关掉（**保留旧核查记录、并标清它已被哪次更新替代**，不走删除）。

## 逐条

| 项 | 旧文声称 | 实测（2026-09-21 回读） | 处置 |
|---|---|---|---|
| **`§18.1`** | 「物种层缺省支的用户语言『仅使用来源』**与现行页面相抵**：两页逐字都是『跟随（INHERIT）』／『跟随』」 | **两页旧串「跟随（INHERIT）」「跟随」均 0 命中**；**「仅使用来源」已在两页在位** | **收口** —— 前提不成立，**无需再连动改页** |
| **`§18.2`** | 「Affinity 缺省支与 `CLEAR` 的用户语言**未落页**（四串 0 命中）；页面现文仍是『跟随物种配置』『恢复为来源值』」 | 旧串**均 0 命中**；「仅使用来源」「仅使用当前来源」**在位**；两页均有「须显式区分」句 | **收口**（REVISION 12 已关，本版并列表出） |
| **`§18.6`** | 冻结卡 `A①-卡3` 逐字「>1＝WARNING（**黄**）」而与《编辑器界面》§1.2／§1.4 相抵 | 卡现逐字为「>1＝WARNING（**红标，不阻断**）」⇒ **与页面同向** | **收口** —— 「WARNING（黄）」已不存在 |
| **`§18.8`** | 「25 个 Structure slot」（只有 paste 有这一处） | 卡现逐字写明「**按当前 Source 动态生成 StructureType 字段控件（不固化字段数量；现行实例 25 是数据/记录层的查表键数，不得反读成 UI 的固定字段数）**」 | **收口** —— 「25」不再作为固定 contract |

**未动的**：`§18.3`／`§18.4`／`§18.5`／`§18.7` —— 它们是**仍在的「页面无出处／口径不一致」登记**（本就该长期挂着），**不是"已闭合却还挂着"**。

## 与 `contract-cards.md` L3 的关系

L3 说「Current 已落并回读、阻塞解除」—— **本版之后，§18 侧不再有与之相抵的旧句** ⇒ **包内状态矛盾消除**。
（你们声明「不裁定内部 Current 哪个说法为真」—— 我们按同一口径处理：**只对齐包内两处状态**，不替 Current 定真伪。）

**通过判据**见 `REVIEW-PROMPT.md` §六（两层门）。
