# REVISION 15 — 处置复审 `03:20` 的**另两条**（`FIG-PACKET-01` 已 sweep；`CXR-STATUS-01` 列待裁）（2026-09-21）

对齐 head：`8c149ce`（REVISION 14）。

## 零、先认一句

复审在 `59a4033`（REV 13）上列了三条。**REV 14 只处置了一条**（`CXR-ROLE-UI-01`）。

⚠️ **原因是：REV 14 是在没有拉 PR 评论端点的情况下出的** —— 它处置 `CXR-ROLE-UI-01` 是**顺着我们自己的那条线**（`[04]` 落法从"追加两枚 chip"改判 F）走的，**不是回应复审判项**。⇒ **另两条一直开着，本版补上。**

---

## 一、`FIG-PACKET-01`（摘要回退）—— **属实，已 sweep。而且比复审看到的更多：四处，其中一处最毒。**

`figma-current.md` 当时有**四处**停在 `[04]` 之前：

| 处 | 原文（陈旧） | 处置 |
|---|---|---|
| **§五 `FIG-02` 行** | 「Role 控制条**共 7 枚 chip**」＋ `242:672` `层级 ▾ 物种层 · 默认（各行继承）`／`242:674` `桶层操作 ▾` | **改成现状**：**9 枚**、分两组、两条分隔线，并指向 §十 |
| **§七 组总则摘要** | 「Policy 控制条（**7 枚 chip**）」＋同一串陈旧枚举 | 同上 |
| **§九**（`R4-FIG-02` 当轮记录） | ASCII／对照表／回读数字**都停在那一轮**，而标题没限定 | **加限定**：标题补「**本节只记那一轮的当时结果，已被 §十 取代**」＋开篇 ⚠️；**正文保留**（那确是一轮的历史，不是现状） |
| **§九 末的「已知表征缺口」** | 「**物种那一组不列操作 ⇒ 图上没有表示「物种层 Role op（`INHERIT / SET`）怎么改」。本轮不补**」 | **关闭** —— 该缺口已被 `[04]` 补上（那两枚 chip 就是它要的表征）；并写明**当初「需先定记号形制」的前置不再需要**（形制照本行侧，**未发明新记号**） |

⚠️ **第四条才是最毒的，也是复审那条最该被记住的部分。** 那是一条**我们主动说出来的缺口**。它被修好之后**没人改这句话** ⇒ **包在"低报"自己的状态**，而**复审会拿这句话报一条我们已经修好的 blocker**。

⇒ 这正是「**自陈缺口＝一条成立的阻塞**」的**镜像**：那边是**自陈未完成、凭空造出一条阻塞**；这边是**自陈失效、把已修的说成没修**。**两边的根是同一条：自陈句不与处置同轮更新。**

**并新增 §十**：`[04]` 落法的**现状节**（九个 chip 逐字、几何回读、中间态三形态留痕、随包图与两条推送前置）—— **控制条现状一律以 §十 为准。**

**§十 里另更正一条我们自己的估算**：§九 曾写「补了会把控制条撑到 **~820**」。**估偏了** —— 那是按「在一个 box 里追字」估的；真做成**两枚独立 chip**，物种组 **147**、条右缘 **5454**。**差别来自"形态不同"，不是"字多字少"。**

---

## 二、`CXR-STATUS-01`（卡 2 状态）—— **属实，但本版不改；列待裁**

**先改一个说法**：复审写的是「卡 2 文件状态**反而回退**」。**我们核下来不是回退** —— `contract-cards.md` 的 **L3 与 L45 彼此自洽**（两处都排除 `A①-卡2`），文件内部没有相抵。

**真正的相抵是：L3 自己写的「恢复条件」已经被满足、并且已经在同一个包里收口了。** L3 逐字：

> ⚠️ **`A①-卡2` ＝ 已裁、但暂不作为无条件执行依据** —— 它所需的用户语言**尚未落入 Current 页面**、且 Current 仍保留相抵旧串（汇编 §18.1–2）⇒ **落到页后即恢复**

而汇编 **§18.1／§18.2 现在都带「已收口」标注**，§18.2 还逐字引**记录页 §312 裁 `XR-F-07` 的「落到页后即恢复」**。

⇒ **这是「条件陈旧」，不是「状态回退」。**

**为什么本版不改它**：**要不要据此把卡 2 恢复成冻结，是记录页 §312／§265 那条裁定的处置** —— 那是**文件头状态行**，改它等于替裁定拍板。⇒ **列待裁**；本版只把事实摆清，不动那一行。

---

## 三、本版不动

- **不动 Notion**。
- **不动图**（本版只改 `figma-current.md` 的文本；随包三张图仍是 REV 14 那一版）。

**通过判据**见 `REVIEW-PROMPT.md` §六（两层门）。
