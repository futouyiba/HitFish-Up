# REVISION 16 — 关闭本 head 上**唯一仍被复审标为 OPEN 的判项**：`CXR-STATUS-01`（卡 2 恢复）（2026-09-21）

对齐 head：`98e6bf0`（REVISION 15）。

## 零、本版做什么

复审 `03:20` 那三条：REV 14 处置 `CXR-ROLE-UI-01`，REV 15 处置 `FIG-PACKET-01`。**`CXR-STATUS-01` 是剩下那一条，也是本 head 上唯一仍被复审标为 OPEN 的项。本版把它关掉 —— 而且不是靠让步，是靠一条既有的裁定。**

## 一、判据来自**记录页 §312 ⑧**（既有 Owner 裁定），不是本版新裁

§312 ⑧ 逐字：

> **`XR-F-07`（Owner 裁：降卡2 的状态）** ⇒ 批次①其余六卡仍「已冻结 v1.1」；**`A①-卡2` ＝ 已裁、但暂不作为无条件执行依据**，**落到 Current 页后即恢复**。

⇒ **恢复条件 ＝「落到 Current 页后」**，是一个**已裁定的动作**，不是待裁的新问题。**本版做的是核实前提，然后执行。**

## 二、前提核实：**打到两页本体上逐字回读**（不读我们自己的汇编）

⚠️ 这一步必须打**页**，不能打汇编 —— 汇编是派生物，它自称「已落」不算数。

| 检查 | 《编辑器界面》§1.2 | 《编辑器心智模型与 IA》§2 |
|---|---|---|
| 旧串「跟随（INHERIT）」 | **0** | **0** |
| 旧串「跟随物种配置」 | **0** | **0** |
| 旧串「恢复为来源值」 | **0** | **0** |
| 「仅使用来源」 | **1** | **1** |
| 「仅使用当前来源」 | **2** | **2** |
| 「沿用物种操作」（缺省名） | 2 | 3 |

且两页**各自写明**「沿用物种操作」（无记录）与「仅使用当前来源」（CLEAR）**是两个动作**：

- 《编辑器界面》§1.2 逐字：「桶（覆盖层）须**显式区分**「沿用物种操作」与「仅使用当前来源」两个动作，不合并成一个模糊的「恢复」。」
- 《编辑器心智模型与 IA》§2 逐字：「「沿用物种操作」（无记录）与「仅使用当前来源」（CLEAR）是两个动作。」

⇒ **前提成立 ⇒ 依 §312 执行恢复。**

## 三、处置

- **`contract-cards.md` 文件头**：`A①-卡2` **恢复为「已冻结 v1.1」**；**批次①读作七张**。**旧注不删，标 `⛔ 旧注（已作废，保留作留痕）`**，并附本次逐字回读作收口依据（＝「留一度 X ＋ 收口 Y」）。
- **`contract-cards.md` Part 2 标题**：六张 → **七张**。
- **`component-contract-consolidated.md` §18.2 收口注**：精确化一处**引文归属**（见 §四）。

## 四、顺带修正一处**引文归属**（我们自己写的）

§18.2 收口注原先写「**两页均写明**「须显式区分「沿用物种操作」与「仅使用当前来源」，不合并成一个模糊的「恢复」」」。**回读后精确化**：**只有《编辑器界面》§1.2 是逐字**（原句为「桶（覆盖层）须**显式区分**…两个动作，不合并成一个模糊的「恢复」」）；**《编辑器心智模型与 IA》§2 是同义的另一句**。

⇒ **这不是"结论错"，是"把两页说成同一句"。** 但它值得记：**「均／都」是最容易过头的量词** —— 一句真话被加上「两页均」，就变成了对另一页的**未经核实的**断言。

### ⚠️ 附一条方法学更正（写本条时我当场犯的）

我最初用**不带标记**的串去 grep 那两页，得「须显式区分」**0 命中**，一度判「汇编引文疑似伪造」。**错的是我的探针**：该页那句里是 `须**显式区分**`，**内联粗体把中文串切开了**。

⇒ **判据串必须与被测物同形；Notion 正文里的标记会切断字符串。** 改用不带标记的短串（「不合并成一个模糊的」）即命中。**这不是第一次、也不会是最后一次：同一晚我先犯过一次假阴性（探针路径写错），这是第二次（探针串与物不同形）。**

## 五、另两条的现状

- **`CXR-ROLE-UI-01`**：REV 14 已处置（`[04]` 落法改判 F）；复审在 `8c149ce` 上判 **closed**（static-projection scope）。
- **`FIG-PACKET-01`**：REV 15 已 sweep 四处；**本 head 尚无复审裁决**。

## 六、本版不动

- **不动 Notion**；**不动图**（随包三张仍是 REV 14 那一版）。

**通过判据**见 `REVIEW-PROMPT.md` §六（两层门）。
