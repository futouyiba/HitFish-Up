# Review 请求｜编辑器 UI Component Contract（2026-09-20 快照）

你在审一份**已经收口过一次**的组件契约。材料见本目录其余文件。**请只审、不要改、不要调用任何写工具。**

---

## 一、背景（你需要知道的三件事）

1. 本项目有**两套并列的分支材料**：你自己早先写的一份「Component 思路稿」（推演体、中途自我推翻过若干处），以及你在 `working/ui-contracts/2026-09-20-fcf-0340b-ui-component-contract-phase1.md` 里那份**分节建档**版。
2. 这两份已被**对拍合并**成一份《组件契约（现行）》（本目录 `component-contract-consolidated.md`），规则是：**以 commit 那版的骨架为底，用 paste 那份补它没有的；两份不一致的单列一节；被推翻的不进正文。**
3. 与此同时，**冻结卡**（本目录 `contract-cards.md`）是这套契约的**执行投影**；Figma 侧的现状见 `figma-current.md`。

**这三样东西的关系**：契约＝"现在是什么"；冻结卡＝"实现按这个做"；Figma＝"图上现在画了什么"。
**权威不在本目录**：本目录是**派生物**，冲突时以项目内的 Current 文档与裁决记录为准（本目录不含其内部链接，故你只能按**内容自洽性**审）。

---

## 二、请你审这六件事（按优先级）

1. **忠实性**：`component-contract-consolidated.md` 是否**忠实地**综合了那两份材料？**有没有把你中途推翻过的东西留在正文里**（这是本次合并的首要风险）。
2. **§17「两份不一致」那 8 条**：每条的裁决（裁在哪一侧）**是否成立**？特别是**方向相反**的那两条（②`SET` 越出档位是否自动改档；③`ADD` 的 tier metadata 两侧同向但都与现行相抵）。
3. **§19「待命名」**：把草稿里的标识符与 16 个组件名**一律弃用、改用页面已有说法**（如 `FieldValueRow` → `FieldValueControl`）—— **这个处理会不会丢掉必要信息**？（判据：**下游是否会因此发明第二套名字**）
4. **§18「未核 · 待裁」**：逐条看有没有**你判断其实已经定了、或其实定不了**的。
5. **与实现的落差**：契约里哪些是"已裁但还没落成 UI"、哪些"根本不需要落"？**有没有哪种说法会让实现方做出与契约相反的东西**？
6. **自洽性（四条护栏）**，逐条过一遍看有没有违反：
   - **同一断言只应有一处**（同一件事四处说法必须逐字同形）；
   - **双入口、单 Truth**（组件卡与 Policy 区读同一份；**不许两份 state 再互相同步**）；
   - **粒度**：数值 override 按 bucket；**Role 恒为 row-level**；
   - **同源 ≠ 同步**（判据：改一处，另一处**不经过任何同步代码**就变）。

---

## 三、输出格式（严格遵守）

**只输出 finding 列表**，每条：

```
ID: F-01
范围: 文件 + 小节/行号锚（给内容锚，不要只给行号）
严重度: BLOCKER | MAJOR | MINOR | NOTE
类型: 相抵 / 缺口 / 模糊 / 缺出处 / 措辞 / 其它
依据: 引材料里的**逐字**（两侧都给，若是指出相抵）
建议: 一句话；**不写实现方案**
```

**四条纪律（这条最重要）：**
- **逐条给出处**；给不出出处的结论**标「未核」**，不要写成判断。
- **发现相抵先报、不要自行调和**（"两侧各有可取、合成一个新形态"这种结论**不要**你自己下）。
- **不许发明新机制、新术语、新标识符**（本族标识符按字节精确；用一个材料里不存在的串＝让下游造第二套名字）。
- **不要重述材料**（不要摘要、不要复述你已经认同的部分）。**只报问题。**

**最后加一节**：`总评` —— 三句话以内，只回答"这份契约能不能作为实现依据"（能／不能／在哪一条上不能）。

---

## 四、边界

- **你不是裁定人**。产出是 finding；裁决与落笔在各线 Owner 手里。
- 不要因为"看着能优化"而提**排版／措辞偏好**类 finding（除非它会造成误读，那时**归到「措辞」类并说明会怎么被误读**）。
- 材料里凡出现「记录页 §N」「《页名》§N」「冻结卡」这类引用，**指的是项目内的载体，你在本目录里读不到** —— 遇到这类引用，**审"这句话本身是否自洽"，而不是去核那个出处**；**若某条离开那个出处就不可判**，就用 `NOTE` 标出来（那本身是有用的信号）。
