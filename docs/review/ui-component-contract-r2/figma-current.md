# Figma 现状（2026-09-20 快照）

**这是一份"图上现在画了什么"的清单**，供你比对契约。**不含文件链接**（按本项目的对外规则，内部链接一律不进仓）。

**CLEAR 相关文字的版本边界**：本文件的 op 标签、画布结构与回读叙述保留自 [PR #7 固定快照](https://github.com/futouyiba/HitFish-Up/blob/1409a13fde46dcb8d10bae039c26f6a0090bf497/docs/review/ui-component-contract-r2/figma-current.md)，本次未重读 live Figma；它们是图面证据，不另定义 CLEAR。组件语义见[汇编 §3](component-contract-consolidated.md#component-clear)，Policy 语义见[§10](component-contract-consolidated.md#policy-clear)。图中 Role 控制条的「CLEAR 回到模板 raw 值」仅在 Policy 域解读。

帧尺寸约定：**宽统一 1680**；**高两档** —— 作者面一族 `1680×1220`，其余 `1680×1080`。

---

## 一、Current 页的帧（7）

| 帧名 | node | 尺寸 | 是什么 |
|---|---|---|---|
| `01 Affinity Compat Mode Authoring` | `8:2` | 1680×1220 | 主编辑面（空态：未选中任何组件卡） |
| `10 Authoring · 编辑（水温）` | `104:269` | 1680×1220 | 同一作者面的**选中态**（水温） |
| `11 Authoring · 编辑（结构）` | `108:311` | 1680×1220 | 同一作者面的**选中态**（结构） |
| `04 Resolve Preview · Subject + Seed` | `9:2` | 1680×1080 | 只读 |
| `05 Bake Preview · ConditionGroup Trace` | `9:39` | 1680×1080 | 只读 |
| `07 Component Template Workspace` | `22:2` | 1680×1080 | 第二工作区 |
| `08 Fish List · CN / EN / ID` | `22:36` | 1680×1080 | 鱼列表 |

**Structure 的表示**：选中 Structure 后在**右侧 460px 焦点编辑栏**、以**2 列紧凑字段控件**编辑（`108:311`；`structureList2Col` 实测两列 x≈1227/1446，**12 行双列 ＋ 1 行单列 ＝ 25 个 `FieldValueControl`** —— **末行只有一列，不要按「13 行 × 2」算成 26**）。
**旧帧 `47:2`（`Component Value Controls · tier & empty state`，形态是 5×5 卡阵）已移出 Current、进归档页**，带 `[FCF-ARCHIVE:v1]` annotation。

**三栏几何**：对象导航 `250` ＋ 上下文总览 `890` ＋ 焦点编辑 `460` ＋ 边距 ＝ `1680`。

---

## 二、投影区（`PROJECTION｜…`，共 11 组）

**A 线（契约卡的逐卡投影；每组＝一卡的关键状态帧）**

| 组 | 卡 | node | 态数 |
|---|---|---|---|
| `A①-卡1 Source Selector` | Source 选择器 | `153:282` | 七态 |
| `A①-卡2 Operation Control` | 字段操作控件 | `163:275` | **十态**（2026-09-20 由八态增至十态） |
| `A①-卡3 Field Value Editor` | 值输入控件 | `166:275` | 八态 |
| `A①-卡6 Validation·Diagnostic` | 校验诊断 | `170:275` | 八态 |
| `A①-卡4 Effective Value Display` | 有效值展示 | `206:704` | 六态 |
| `A①-卡5 Provenance Display` | provenance 展示 | `206:770` | 五态 |
| `A①-卡7 Autosave Status` | 保存状态 | `206:835` | 七态 |
| `A②-卡8 Template Library List` | 模板清单与别名 | `232:670` | 四态 |
| `A②-卡9 Template Lifecycle` | 生命周期两态 | `232:724` | 三态 |
| `A②-卡12 Reference List` | 引用者列表 | `232:746` | 三态 |

**GPT 线（另一套并行投影，与本清单不是同一批）**：`178:291` 卡4 ComponentCard ／ `178:399` 卡5 Impact Preview ／ `183:484` 卡7 FieldValueControl。

**未投**：`A②-卡10`（模板改值·高影响）／`A②-卡11`（批量换绑）—— 等实现落地后再投。

---

## 三、2026-09-20 的改动（本轮）

**新增 2 帧（`A①-卡2` 组内）**
- **态9 `230:661`**：**五支用户语言** —— 物种层「仅使用来源／调整／设置为」；Affinity 层「沿用物种调整／沿用物种设置为／仅使用当前来源／调整／设置为」；并写明「物种层无操作时，Affinity 缺省支显示『仅使用来源』」「**不显示泛称『沿用物种操作』**」「枚举绝对值项没有『调整』」。
  - ⚠️ 该帧的**取值是示意、非规定**；另一支同理（物种层为 `SET` 时显示「沿用物种设置为」）。
- **态10 `230:673`**：**两种密度并排** —— 折叠（字段名／操作摘要／Effective／Diagnostic）＋ 展开（当前来源／被替代的上级操作／本层操作／Effective／provenance 算式）；**被替代的上级操作用删除线＝视觉降级**，注明只解释 provenance、不参与链式计算；**原地展开、不设二级 drawer**。

**`A①-卡2` 的八态帧内文本批量改**：`跟随物种配置` → 态1「仅使用来源」／态2–8「沿用物种调整」；`恢复为来源值` →「仅使用当前来源」。

**校验分级配色**：`<0` 的 ERROR 与 `>1` 的 WARNING **都是红标**（4 个节点由琥珀改红）。**注意类**的琥珀 badge **未动**（它们不是校验分级）。

**批次②三组新投**：卡8 `232:670`／卡9 `232:724`／卡12 `232:746`（见上表）。

---

## 四、本文件侧的记号与纪律（读图时注意）

- **未实现/未核必须标**：`⚑UNIMPL（GAP-ED-NN）` ／ `⚐UNVERIFIED` ／ `⏸BLOCKED`。**「未标」不等于「已实现」**。
- ★ **作者面 vs 投影面的文字分工（2026-09-21 补，读图前先读这条）**：
  - **作者面（Current 页那 3 帧 `1680×1220` 一族）** —— 画面里出现的串是**作者会读到的串** ⇒ **必须用作者词汇**（《编辑器界面》§1.2「操作的用户语言」＋《编辑器心智模型与 IA》§2 的作者词表）；`INHERIT`／`absent`／`CLEAR`／`SET` **不作作者可见串、也不加括注**。
  - **投影区（`PROJECTION｜…` 各组，`1680×1080` 一族）** —— 画面里出现的串是**给读契约的人的记号** ⇒ **`CLEAR`／`INHERIT`／`SET`／`absent` 在这里是合法的**，且**「作者词（操作令牌）」这种绑定括注正是投影帧该有的东西**（把作者词钉到 op 上）。
  - ⇒ ★ **同一条 finding 落在哪一面，结论会相反**：「作者面出现 `INHERIT`」⇒ **要改**；「投影帧出现 `INHERIT`」⇒ **不是缺陷**。
  - ⇒ **判帧靠 node 前缀 ＋ 尺寸两档**：`108:*`／`104:*`／`8:2` ＝ 作者面（1220 高）；`230:*`（`A①-卡2` 组）／`232:*`／`163:*`… ＝ 投影区（1080 高）。★ **不要只按 node 前缀猜** —— 以本文 §一／§二的登记为准。
- 图上的说明**一律走 annotation，不写 comment**；**不把说明文字画进画面主体**。
- 已知残留：`A①-卡2` 与 `A①-卡3` 之间的**间隙现为 70px**（因卡2 增高所致，**非设计意图**；同列其余间隙 200–240px）。

---

## 五、2026-09-21 本轮改动（`FIG-01`／`FIG-02`／`FIG-03` 收口）

**`FIG-01`（`108:311`／`108:335`）** —— 卡内来源入口从「只有 `SourceLabel` 文本」改为**可辨识入口形态**：**一个框 ＋ 自带文字 ＋ `▾`**（与顶部 `sel_*` 同形），**共 8 个节点、全在 `108:335` 组内**（**组级 overlay，未动母件**）：

| 卡 | 框 | 值文字 |
|---|---|---|
| 水温 | `238:662` `sel_来源__temperature` 124×19 | `243:663` `Temp_Largemouth_Bass  ▾` |
| 结构 | `238:663` `sel_来源__structure` 84×19 | `243:664` `类型名（示意）  ▾` |
| 觅食水层 | `238:664` `sel_来源__feedingLayer` 145×19 | `243:665` `FeedLayer_Largemouth_Bass  ▾` |
| 时段 | `238:665` `sel_来源__timePeriod` 128×19 | `243:666` `period_Largemouth_Bass  ▾` |

**`FIG-02`（`108:335` ＋ `108:364`）** —— **`absent` 与 `CLEAR` 并列出现在同一帧**：卡侧 4 枚层签（`242:661`／`663`／`665`／`667`，内容 `物种层`）；Policy 侧**一条「Role 控制条」，`[04]` 之后共 9 枚 chip**（**分两组、两条分隔线**）：**`242:670` `水温 · Temperature 的 Role`**（**从"通用"改成"哪一个具体的 Role"**）／**物种组**：`266:661` 分隔线 ＋ `242:672` **`物种层`** ＋ **`282:661`（新）`仅使用来源`** ＋ **`282:663`（新）`设置为`**／**本行组**：`266:662` 分隔线 ＋ `242:674` **`本行覆盖`** ＋ **`252:662` `目标行：LAKE_A × LARGEMOUTH_BASS/Q3`** ＋ `242:676` `absent 跟随继承` ＋ `242:678` `CLEAR 回到模板 raw 值` ＋ `242:680` `SET 设置`。
⇒ **两个作用域各自列自己的可用操作**（物种层 `INHERIT`／`SET`，本行 `absent`／`CLEAR`／`SET`）⇒ 卡上 Role 角标 ＋ 层签 ＋「继承／已覆盖」**并读为「结果值 · 改哪一层 · 该层可用操作」**。**逐条现状与回读见 §十。**

**`FIG-04`（`108:311` / `108:364`，2026-09-21 `ADJ-03` 落图）** —— 控制条里补上**物种层 Role 的记录态位**：**新增一枚只读派生 badge `308:661` `roleEntry_sp_roleState`**（文字 `308:662` ＝ **`Role 已钉住`**，48 宽 ⇒ 框 **60**；样式逐项抄 `242:674`「本行覆盖」＝ 浅蓝底 ＋ slate 边 ＋ r5 ＋ Inter Regular 9 ＋ 蓝字，**未引入新色**；★ **选浅蓝而非灰**：本文件语法 **灰 ＝ Scope、白底蓝字 ＝ 可点 Action、浅蓝 ＝ 状态陈述**，用灰会让 State 像 Scope）。
⇒ 两态：**`INHERIT` ⇒ 该位缺席；`SET(…)` ⇒ 显示 `Role 已钉住`**（**由 durable op record 是否存在派生，不新增第二份 durable UI state**）。
⇒ 机械：槽 `242:672` 回内容宽 **60 → 39**（`242:671` 逐项回到写入前）；右侧 8 元素 ＋ 1 分隔线再移 **+45**（净 +66）。
⇒ **回读**：**全排 11 处空隙全部 ＝ 6**、**两两相交 0**、行右端 **832 ≤ 838**、帧 **854×122** 未变、子节点 **12→13**。
⇒ **ann**：新基线 **943 字 ／ `sha256[:10]＝b11edfb31e` ／ `&`＝0**（旧 546 / `013d7004b126` 因追加 397 字而变 —— **设计使然**，不是被覆盖）。
⇒ ⚠️ **两态各需一张图**（复审明说「一个静态 `SET` 状态不能代替所有状态」）⇒ **两态都已随包**：`SET` 态 ＝ `img/0.3.4.0-B-policy-block-108-364.png`；**`INHERIT` 态（该位缺席）＝ `img/0.3.4.0-B-policy-block-108-364-INHERIT.png`**（**与 SET 同轮导出**）。
★ **`INHERIT` 那张按「无预留槽位」渲**（依据 Owner 2026-09-21「以后再考虑固定槽位、现在不为这件小事延长设计」）⇒ **不得读成「两态位置一致」**（实测整排左收 66px）。

**`FIG-03`（`230:673`）** —— `230:691` 从连续链改写为**「来源 → 当前层操作 → 当前值」三段**（不再把被 `CLEAR` 替代的上级 `ADD` 画进链里）；该帧 annotation 就地更正、带两步留痕。

**页节点数**：`2285 → 2309`（Δ=+24，分两批落）；**`ADJ-03` 落图再 +2 ⇒ `2318`**。
★ **这个数必须连开关一起读**：`figma.skipInvisibleInstanceChildren` **开 ⇒ `2318` ／ 关 ⇒ `2330`**（差 12 ＝ 被该开关藏起来的实例子节点）⇒ **任何节点计数都报「数 ＋ 该开关的实读值」**，否则会把开关造成的差读成内容变化。

**三处有意保留、勿顺手改**（已逐字登记在 `108:311` 组总则）：
1. Policy 控制条**紧贴 `108:364` 下缘**（内容到 y=121、容器高 122）—— **容器高度未改是有意的**：未裁切⇒可读；间距整齐不是判据；加高会吃掉与 `108:373` 之间的 12px。
2. **卡侧只显示物种层**；桶层 `absent`／`CLEAR`／`SET` 只在 Policy 控制条里作选项 —— **层面的结构限制**（**物种层没有 `CLEAR`**），**不是漏做**。
3. 卡内四个 `SourceLabel` 已不在实例里 ⇒ **若对该实例 `resetOverrides`，母件标签会回来并与四个文字节点重影**。

## 六、随包图像（**2026-09-21 重导**；整帧、**不含 annotation**；★ **不再全为 `scale=1`** —— 理由见下「为什么有一张换了 scale」）

| 文件 | node | 像素 | 字节 |
|---|---|---|---|
| `img/0.3.4.0-B-editor-structure-108-311.png` | `108:311` | 1680×1220 | **183422**（`ADJ-03` 落图后重导；**与上一版同为 REST 管线** ⇒ 182744 → 183422 **是内容差**，不是换管线。sha256[:12]＝`da385a1b5bec`） |
| `img/0.3.4.0-B-card2-state10-230-673.png` | `230:673` | 890×150 | 24410（**逐字节未变**） |
| `img/0.3.4.0-B-policy-176-283.png` | `176:283` | 820×92 | 13485（**逐字节未变**） |
| `img/0.3.4.0-B-policy-block-108-364.png` | `108:364` | **1708×244**（`scale=2`） | **51787**（**换图**：旧 854×122／21871 那张**画面陈旧** —— 它导出于 badge 落地**之前**；sha256[:16] ＝ `5b91e048d1ce3bea`；`pHYs 2835` ⇒ 与包内同一条 REST 管线 ⇒ **是内容差、不是换管线**） |
| `img/0.3.4.0-B-policy-block-108-364-INHERIT.png` | `108:364` | **2135×305**（`scale=2.5`） | **66070**（**新增：`INHERIT` 态**，与 SET 同轮导出；sha256[:16] ＝ `afe41f54260139a1`；`pHYs 2835` ⇒ 同管线。★ 按「无预留槽位」渲） |

★ **为什么有一张换了 scale（不是偷懒，是缓存）**：**REST 渲染按 `(file, node, scale, format)` 缓存**，而 `108:364` 的 **`scale=1` 那一格在 badge 落地前就被烤过** ⇒ 此后**任何 `scale=1` 重导都拿回旧画面**（试尽 `contents_only`／`version=<当前版本>`／连字符 id／`scale=1.0`·`1.00`·`1.000001`·`0.999999` ⇒ **全部返回同一 sha**）。
⇒ **两条纪律（本族新记）**：① **「我重导了」≠「你拿到的是新画面」——必须像素对拍**（拿旧图与它做差值），**不能只看「我把导出接口调了」**；② **同管线、同内容，字节也可以不同**（`scale=2` 51787 vs `scale=2&use_absolute_bounds=true` 50461，**逐像素完全相同**）⇒ **字节只作粗信号，内容判据一律像素对拍**。
⇒ 与本节那条「**跨管线比字节无效**（`pHYs 1000` vs `2835`）」合起来成一句：**字节不是内容判据 —— 管线会改它、缓存也会改它。**
⇒ ★ **另一条登记**：包内那张**旧** `108-364`（854×122／21871）在视觉上**≈ `INHERIT` 态**（badge 本就缺席）—— 但它是 **`ADJ-03` 改动前**的一轮导出，**不能当 `INHERIT` 态的证据**；`INHERIT` 证据用新随包那张（**与 SET 同轮导出**）。

★ **★ 2026-09-21 补拍说明（一处此前没被发现的口径错）**：`176:283` 的 name 是 **`SpatialOpportunityPolicy`**、type 是 **`COMPONENT`**，位置在 **`x=1040, y=6040`** —— 它是**母件**，**不在作者面上**。而本轮 `[04]` 改动的那三枚 chip 住在 **`108:364`（`SpatialOpportunityPolicyBlock`，FRAME，`x=4628,y=335`，在 `108:311` 内）**，其 overlay 节点是 `242:676`／`678`／`680`。
⇒ **所以旧的第一版「Policy 图」拍的从来不是作者面那一块** —— 这也解释了「④ 改了而它逐字节未变」。
⇒ **两张都留**（母件是合法对象、作者面那块才是投影证据），**但引用 Policy 现状时请用 `108:364` 那张**。

**⚠️ 一条判图纪律（本轮实测出来的）**：**「字节变了」不等于「内容变了」。** 三张图从上一版重导后，`108:311` 有**真内容差异**（`y=442–455`、`x=331–1123`，恰好一行 chip 的高度 —— 即 `Role 入口` → `水温 · Temperature 的 Role` 与新增的「目标行」）；而 `176:283`／`230:673` 的差异**只是圆角抗锯齿**（改动像素 64／100，**全在四角行**，每行 2–16 px）—— **那是渲染层差异，不是内容变化**。
⇒ **比图时不要只比字节/哈希；要落到「差异像素落在哪一行哪一列」**。

**⚠️⚠️ 再一条、更要紧：比字节之前先确认「同一条导出管线」。**（2026-09-21 由 blob 尺寸逐 commit 追出，**含包自己的历史**）

| 图 | `e44e91c`（08:41，REV-3 期） | `41c641f`（09:52，REV-6） | `5591f24`（10:49，REV-11） | 现行 |
|---|---|---|---|---|
| `230:673` | **24055** | 24410 | 24410 | 24410 |
| `176:283` | **13285** | 13485 | 13485 | 13485 |
| `108:311` | **171330** | 174828 | 175020 | **183422** |

- **REV-3 期三个数（171330／24055／13285）是 MCP `get_screenshot` 产的**（同节点同管线重导**逐字节复现**，2026-09-21 实测）；**REV-6 起换成 REST `/v1/images?scale=1`**。
- ⇒ **REV-6 那一轮，`230:673`／`176:283` 的 24055→24410、13285→13485 是「换管线」，不是「内容变化」** —— 这两个节点当时没被改过。**上表把这件事记成字节变化，是错的。**
- ⇒ **跨轮比字节只在同一管线内有效。** 两条管线的同内容产物**差约 1.5%**（本例：24055→24410＝+1.48%、13285→13485＝+1.51%），**足以把"没变"读成"变了"**。


**旧版为何是旧的（同一条纪律的另一面）**：上一版的图导出于 `00:40:28Z`，而画布末次写入在 `01:20:36Z` ⇒ **图早于画布 40 分钟** ⇒ **随包物证必须与声称的改动同批取**。

**四张图 2026-09-21 全部实读**（不照抄旧述）：**`pHYs` 一律 `2835/2835 unit=1`**（＝ **REST `/v1/images` 管线**），`tEXt Software=Figma` ／ `colortype 6` ／ `bitdepth 8`，**且均为恰好 1×**。
⚠️ **本节此前写「原三张同参数（`pHYs 1000/1000`）」—— 那是 REV-3 期 MCP 管线的读数，早已过期**；现按实读更正。**引用任一张图前请先读它的 `pHYs`：`2835` ⇒ REST，`1000` ⇒ MCP。**

**两条通路事实**（供核图的人判可信度，不是缺陷）：

- **另有一张 `141:283`（SECTION）未随包** —— 它**含 section 边界、不等于 `176:283` 那一屏的紧致包围盒**，且导出为 **≈1.02×**（节点 `3800×1848` → 出图 `3880×1928`）⇒ **与上表三张不同参数，故不收**。
- **MCP `get_screenshot` 不放大**：请求 `108:335`（3280）／`108:364`（3416）欲取高倍局部，**回图仍为 1×** ⇒ **这条通路拿不到高倍局部**；先前评审所用「4× 局部」**不是从这条通路来的**（那一路径未核）。

## 七、图上另有 annotation 承载细则 —— 这里给**对外可见的摘要**

⚠️ **Figma 的 annotation 对读本目录的人不可见**（本目录只有文字与图像）。而 `108:311`／`108:335`／`176:283` 上各有一条较长的组总则。⇒ 这里给摘要，**以免"图上写了、读者看不到"**。

**`108:311`／`108:335` 的组总则（摘要）**：

- **Source 两处入口的分工**：**顶部 `templateRow` 的四个来源选择器是「前层」选择器** —— 依据《编辑器持久层契约》§8 首行「每组件一个**前层** Source 选择器」，以及一条 Owner 裁定（**与前层同层、不要求先进入组件展开区才能选、显眼便利**）；**卡内入口**是 §7 的**「就地快速编辑」**。⇒ **两者不是"重复编辑器"**，因为它们**读同一份 Truth**（**双入口、单 Truth**）。
- **本帧是物种层面** ⇒ 卡上角标读的是**物种层那一份 Role**；**物种层没有 `CLEAR`**。
- **卡上三个可读点**：Role 角标（**结果值**）＋ 层签「物种层」（**改哪一层**）＋「继承／已覆盖」（**该层状态**）。
- **Policy 控制条（`[04]` 之后 **9 枚 chip**，分两组）**：**`水温 · Temperature 的 Role`**（哪一个 Role）｜**物种组**：`物种层`（哪一层）＋ `仅使用来源` ／ `设置为`（**该层可用操作**）｜**本行组**：`本行覆盖`（哪一层的覆盖）＋ **`目标行：LAKE_A × LARGEMOUTH_BASS/Q3`**（**哪一行**）＋ `absent 跟随继承` ／ `CLEAR 回到模板 raw 值` ／ `SET 设置`（**该层可用操作**）。⇒ **四件齐：哪一个 Role／哪一层／哪一行／该层可用操作** —— 且**两个作用域各自列出自己的操作**（物种层 `INHERIT`／`SET`；本行 `absent`／`CLEAR`／`SET`）。

**另**：`176:283`（Policy 区）另有一条 —— 「**Policy 区 Role 行 ＝ Authoring Truth**」。

## 八、卡上 Role 角标 —— 为什么它是"三态下拉"，以及为什么它**不做成白场**

**图上的角标**：每张 `ComponentCard` 上有一枚 Role 角标（**74×16**，角色色淡底 `#e3edfb` ＋ 角色色描边 `#2e63b8`，圆角 8），**内含一个矢量三角 `roleCaret`（7×4）**，标签 TEXT 为 `CORE` / `SECONDARY` / `IGNORED`。
⇒ **它是一个三态下拉控件，不是纯状态徽标。**

**依据（页 §1.1 逐字）**：「**AggregationRole｜每个组件卡一个三态下拉：CORE / SECONDARY / IGNORED**」。

**与顶部 `sel_*`（来源选择器）对照 —— 两者都是"入口"、形态**有意**不同**：

| | 顶部 `sel_*`（来源选择器） | 卡上 Role 角标 |
|---|---|---|
| 容器 | 126×20 | 74×16 |
| 填充 | `#ffffff`（白场） | `#e3edfb`（角色色淡底） |
| 描边 | `#d4dbe5`（中性灰） | `#2e63b8`（角色色） |
| 圆角 | 5 | 8 |
| 下拉记号 | 文字字形 `▾`（advance **6px**） | 矢量三角 `roleCaret`（**7×4**） |
| 读起来是 | 输入字段 | 状态角标 |

**⚠️ 一处"可辨识度"与一条已裁规矩的相抵 —— 刻意保留现状**：

让角标「**更读得出是下拉**」的**做法是白场 ＋ 中性描边**；但那会**抹掉角色色区分**，而**角色色是已裁的** —— **记录页 §104 四**逐字：「**角标色保留：CORE 蓝 / SECONDARY 琥珀 / IGNORED 灰 —— 满足 Owner 那条『非常重要信息』的要求**」。

⇒ **当前保留角色色**；**下拉记号以矢量三角承载**。

**量纲对照（两种记号不是同一把尺子 —— 别混比）**：**矢量三角的包围盒 `7×4`** vs **顶部字形 `▾` 的 advance `6px`**（**字形的墨宽不超过 advance，故更小** —— ⚠️ **后半句是排版推导、不是实测**）。
⇒ **无论按 advance 还是按墨宽比，卡上那个都不更小**（这一句的两种读法都成立）。

## 九、2026-09-21 图侧改动：`R4-FIG-02`（两个作用域分开）—— **本节只记那一轮的当时结果，已被 §十 取代**

⚠️ **本节不是现状。** 下面的 ASCII、对照表与回读数字**都停在那一轮**；**两轮之后（`[04]`）控制条再改过一次** ⇒ **现状一律以 §十 为准**。保留本节，是因为**它的「三处有意没动」与「一条已知缺口」两段需要被复盘** —— 其中**那条缺口已在 §十 关闭**，且**当初对宽度的那句估算是错的**（见 §十）。

**Policy 控制条（`108:364`，854×122）分成两组 ＋ 两条分隔线**：

```
[水温 · Temperature 的 Role] │ [物种层 · 默认（各行继承）] │ [本行覆盖] [目标行：LAKE_A × LARGEMOUTH_BASS/Q3] [absent 跟随继承] [CLEAR 回到模板 raw 值] [SET 设置]
```

| chip | 改动 | 填充／文字 |
|---|---|---|
| `242:670` | 保留（上下文：哪一个 Role） | 白 / `#1a1f29` |
| **`266:661`（新）** | 分隔线 `1×14` | `#d4dbe5` |
| `242:672` / `242:671` | `层级 ▾ 物种层 · 默认（各行继承）` → **`物种层 · 默认（各行继承）`** | **`#edf0f5`** / `#637085`（中性灰） |
| **`266:662`（新）** | 分隔线 `1×14` | `#d4dbe5` |
| `242:674` / `242:673` | `桶层操作 ▾` → **`本行覆盖`** | **`#e3edfb`** / `#2e63b8`（角色蓝） |
| `252:662` | 保留 `目标行：…` | 白 / `#1a1f29` |
| `242:676`／`678`／`680` | 保留三操作 | 白 / `#2e63b8` |

⇒ **判读靠结构、不靠读文字**：**`CLEAR` 物理上位于「本行覆盖」组内** ⇒ 点它改的是本行覆盖，不是物种默认。两组的填充用的是**本文件既有色**，**未引入新色**。

**回读**：页节点 `2311 → 2313`（Δ=+2，恰为两条分隔线）｜`108:364` kids `8 → 10`｜右缘 **797 ≤ 854**｜条内两两相交 **0 对**｜转义 `amp 0`｜母件零写入｜容器高度未改｜`ann[0]` 零写入。

**三处有意没动**（本次）：① **容器高度**（已登记的「高度未改是有意的」；条下只剩 15px，加高会吃掉与 `108:373` 的 12px）；② **母件一个没动**；③ **物种那组没补 `INHERIT／SET` 两个操作**（本轮未获要求；补了会把控制条撑到 ~820）。

**⚠️ 一条曾经的表征缺口（那一轮主动说出来的）—— 已在 `[04]` 关闭**：那一轮去掉了 `层级 ▾` 与 `桶层操作 ▾` 两个**控件记号**（它们变成**组头**），后果是 **物种那一组不列操作 ⇒ 图上没有表示「物种层 Role op（`INHERIT / SET`）怎么改」**；那一轮记的是「**本轮不补**；若审阅认为该补，请提出 —— 那时需要先定**记号形制**」。

**审阅提了（`CXR-ROLE-UI-01`），`[04]` 补上了**：物种组现在列出 **`仅使用来源` ／ `设置为`** 两枚 chip（**形制照本行侧既有 chip，未发明新记号**）⇒ **当初那条「需先定记号形制」的前置不再需要。** **现状与回读见 §十。**

---

## 十、`[04]` 落法（对 `CXR-ROLE-UI-01` 的答复）—— **控制条现状以此节为准**

**要解决的问题**：`CXR-ROLE-UI-01` 要的是「Role 入口**能表达 operation**」。物种侧原本只有 `物种层 · 默认（各行继承）` —— 一个**状态**格，**没有 op**。

**为什么不按原候选落**：控制条内容右缘 **5425**（**相对容器起点 4628 ⇒ 797**），余量 **57**；追加两枚 `仅使用来源` ＋ `设置为` 要 **＋108** ⇒ **905 > 854**，超 51。**854 不是随手定的** —— 它是本帧**内容列宽常量**：`108:312` W4B Main Content 890 宽、内边距 18×2 ⇒ 内容列 **4628..5482**，**再宽就进右栏**。

⚠️ **顺带更正 §九 里的一句估算**：那一轮写「补了会把控制条撑到 **~820**」—— **估偏了**。那是按「在一个 box 里追字」估的；真做成**两枚独立 chip**，物种组共 **147**，条右缘 **5454**。**估算与实做的差别来自"形态不同"，不是来自"字多字少"。**

**落法**：物种格 `物种层 · 默认（各行继承）`（118）改成**三件** —— `物种层`（**39**，留作**作用域标**）＋ **`282:661` `仅使用来源`**（**57**）＋ **`282:663` `设置为`**（**39**）。两枚新 chip **形制照本行侧**（`figma.createFrame()` ＋ 文字子节点，`fills`／`strokes`／`strokeWeight`／`cornerRadius`／字号／文字色**整份复制自行侧**）⇒ **未引入新色、未发明新记号**，**未新增除这两枚以外的节点**。

**⚠️ 中间态留痕（这一处落过三个形态，按「留一度 X ＋ 收口 Y」记）**：

| # | 形态 | 判 |
|---|---|---|
| ① | 派单原案：物种格后**追加两枚 chip** | **作废** —— 超右缘 51 |
| ② | 首交付：**一个 box 里一串文字** `物种层 · 仅使用来源 · 设置为`（126） | **退回** —— 见下 |
| ③ | **收口：三件**（39 ／ 57 ／ 39） | **现行** |

**② 为什么不算**：**`CXR-ROLE-UI-01` 要的是「形」，不是「描述」。** 本行侧三枚是**独立 chip**（`roleEntry_opt_absent`／`_clear`／`_set`）—— 那是**控件**；② 是**一个 box 里一串文字** —— 那是**说明书**。⇒ **两作用域不成对**，而**本条 strip 的两条分隔线（`266:661`／`266:662`）存在的全部意义，就是把两个作用域对照起来**。`·` 在本条是「作用域·状态」的**粘连符**，拿去分隔两个**并列可选项**，读出来是事实清单、不是二选一。

**九个 chip 的逐字（现状）**：

```
[水温 · Temperature 的 Role] │ [物种层] [仅使用来源] [设置为] │ [本行覆盖] [目标行：LAKE_A × LARGEMOUTH_BASS/Q3] [absent 跟随继承] [CLEAR 回到模板 raw 值] [SET 设置]
```

**几何回读（绝对坐标，2026-09-21 直读）**：条右缘 **5425 → 5454**（上限 5454；容器 5482，**余 28**）｜`108:364` kids **10 → 12**｜`108:311` 子树 **415 → 419**（Δ=+4 ＝ 两枚 chip 各 frame ＋ text）｜**页节点 `2313 → 2317`**｜容器 **854×122 未动**｜条内两两相交 **0 对**｜`allInside = true`｜**`108:364` annotation 546 字 / sha12 `013d7004b126` / `&`＝0 逐字未变**（其语义仍成立 ⇒ **不动**）｜母件零写入｜转义 `amp 0`。

**⚠️ 已知偏离（画布 ↔ 契约，待画布侧改）**：

- **`108:311` 顶部那一行的文字仍是 `模板`**，而契约要求这个入口叫「**来源 / Source**」——**因为合法来源里含 `SPECIES_CONCRETE`，它不是模板**。⇒ 偏离在**画布**，不在契约；**本文件如实描述画布现状**（不替它改成契约的措辞），**改法归 Figma 线**。

**随包图同批换过**：`img/0.3.4.0-B-editor-structure-108-311.png` **175020 → 175189 → 183422**（**末一次是 ADJ-03 落图后重导，与前一版同为 REST 管线**）（整帧、**不含 annotation**；**当时**另两张**逐字节未变** —— ★ **该句已过期**：`108-364` 已于同日**因 `scale=1` 缓存回吐旧画面**而**换图并换 scale**，见 §六）。**两条推送前置**：以两个**未动节点**标定管线（重导与基线**逐字节相同含 sha**）＋ `annotation-diff.py` **退出码 0**。**修订留痕见 `REVISION-14.md`。**

---

## 十一、已知漂移：共享母件 `93:54` 的作者可见串修正 → 顶栏右移 53px

**裁定：`ADJ-FIG-ARCHIVE-01` ＝ C_NARROW（Owner 2026-09-21）。**

【漂移已知】**顶栏 strip 的 10 个 chrome 节点**因共享母件 `93:54` 的作者可见串修正而右移 **53px**（按钮宽 `47 → 100`、徽标 x `+53`）—— 涉及 **Current 页 4 个实例 ＋ 归档 2 个实例**（`93:215` ⊂ `47:2`；`180:493` ⊂ `180:392`）。

**保护性证据未受影响**（裁定明列）：**5×5 legacy 控制拓扑** ／ **标签·状态·交互语义** ／ **结构关系** ／ **可行性证据** ／ **既有 node id 与按 id 的引用**。

像素级历史外观见 **FrozenSnapshot**（冻结版本 `2401504721345405021`）—— **活体归档帧不再需要在「非证据性 chrome」上逐像素一致，那份责任由 FrozenSnapshot 承担**。

⚠️ **未来任何触及上述保护性证据的共享母件漂移，不在该裁定覆盖内，必须重新评估。**

★ 本节是**一句注记**，**不是**漂移追踪系统（裁定明禁另造）；**归档侧同一句由画布侧落**。
