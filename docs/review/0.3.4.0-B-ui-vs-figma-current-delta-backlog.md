# 编辑器 UI vs Figma Current 7 帧 —— 逐帧缺口清单（delta backlog）

**用途**：Owner 目标第 2 条「UI 达到 figma 设计」的开工底稿。只读分析产出，未改任何仓、未写 Figma。

## 基线与读取锚

| 侧 | 锚 |
| --- | --- |
| 编辑器（A 侧） | 仓 `/Volumes/Mac DS - Data/SharedProjects/preCompute/FishHabitEditor-0.3.4.0-b`，分支 **`futou/hitfish-0.3.4.0-b-batch28`（head `bba7db1`，2026-09-22）**，盘上检出干净（`src/ui` 无未提交改动）。备选 `futou/adj11-writeback-arm`（`bcf2233`）与本基线的 UI 面差异仅为 app.ts 一处写回闸门 + 一条状态消息字符串（`git diff bba7db1 bcf2233 -- …/src/ui` = app.ts 40 行），**不影响本清单任何条目**。 |
| Figma（B 侧） | Current 假图（W4B B Current）· fileKey 不入公开仓（见内部登记），共 7 帧：`01 Affinity Compat Mode Authoring`／`10 Authoring · 编辑（水温）`／`11 Authoring · 编辑（结构）`／`04 Resolve Preview · Subject + Seed`／`05 Bake Preview · ConditionGroup Trace`／`07 Component Template Workspace`／`08 Fish List · CN / EN / ID`。读取通路＝REST `/v1/files/{key}/nodes`（`characters` 逐字 + `absoluteBoundingBox` 几何 + fills/style），**live 读取于 2026-09-22，未钉 version 快照；读取时点 REST `version` ＝ `2401856974148600738`**（取自当轮保存的 /nodes 响应体）。 |

**方法与限制（如实）**：
- 帧上 annotation 是需求载体，未当 UI 规格；本清单只取画面主体 TEXT。
- 本会话图片通路不可用（MCP 截图已下载为合法 PNG，Read 渲染返回 `[Unsupported Image]`）；视觉核验改以 REST 的逐字文案 + 节点几何 + 字号/颜色完成——对文案/布局类判据反而更精确，但**纯视觉项（间距、留白、圆角观感）未逐项核**，样式级条目只报有数据背书的。
- 已核实一处「字段名骗人、值对」：fixture `scientific_name` 列实值为英文名（`Redear Sunfish` / `Yellow Perch`…），编辑器鱼列表第二级显示（`rail`/`fishList` 读该字段）与图 `Largemouth Bass` 第二级**内容一致**，不列为 delta。
- 既有对齐成果（三栏骨架 250/890/460、帧宽 1680、内容列宽 854=890−18×2）编辑器侧均成立（`src/ui/shell.ts:48-74`），未重复报。

## ⚠️ B 侧帧代际（读清单前必看）

Current 三张作者面帧**不是同一代**：
- （01）与（10）：组件卡为手绘 frame，卡上 SourceLabel 只读、无 Spatial Opportunity Policy 块；（10）在（01）之上已有「编辑中 →」标记与 archiveLevelBlock。
- （11）为**最新形态**：组件卡改为 `ComponentCard` 组件实例 + 卡上 **Source 选择器**（`sel_来源__*`）+ 每卡「物种层」层级标签 + **SpatialOpportunityPolicy 块** + roleEntry 展开态行（`roleEntry_*` 一族）。
⇒ 对齐目标以 **11 帧形态为准**（且该区域正被 #61 修订）。01/10 帧里与 11 帧冲突的局部以 11 帧为准；本清单在相应条目注明。

---

## 〇、共享构件（全帧共通）

### 顶栏 topbar

**T1 · Publish 按钮缺失**（控件类）
- 位置：Figma 顶栏 btn_Publish（'Publish'，作者面/模板面 actions=full 五帧都有）；编辑器 [index.html · 顶栏按钮（#btnReset／#btnExport／#btnPreset）](../../../preCompute/FishHabitEditor-0.3.4.0-b/index.html) —— 无 #btnPublish（@ preCompute master `23cf6e2`，2026-09-23 实读）。
- 差异：图上作者面顶栏动作集 = `丢弃未保存的改动` / `Publish` / `导出` / `Bass 预设`；编辑器无 Publish 位。写回通路本身已有（`app.ts:1295` `runWriteBack`），但顶栏级 Publish 编排未定。
- 量级：**控件级** ｜ 可开工度：**高参裁定暂不实现**（2026-09-23，经开发线直投——未闭合产品语义、暂不实现；明令：不得把工作区 `#publishBar` 解释成顶栏执行器、不得新增收名弹层／复用名值规则。此前一度判缺口＝该补，依据四证：契约 A1 行「顶栏（…动作…/Publish）」逐字／画布顶栏 Publish ×3（actions=full 作者面）／N2 态4 annotation「Publish｜顶栏」逐字／§十 无已裁留痕；原「形态〔导航／执行器／复用已填名〕待裁」随裁定收束）。

**T2 · Reset 按钮文案**（文案类）
- 位置：Figma 顶栏 Reset 按钮（**'丢弃未保存的改动'**）；编辑器基线 [index.html · #btnReset](../../../preCompute/FishHabitEditor-0.3.4.0-b/index.html) 文案 'Reset'——@ master `23cf6e2`（2026-09-23）已为 '丢弃未保存的改动'，与图逐字一致。
- 差异：基线时图逐字 **'丢弃未保存的改动'** vs 编辑器 **'Reset'**；现已对齐。
- 量级：**文案级** ｜ 可开工度：**已落**（@ `23cf6e2`：`#btnReset` 文案已为图逐字）。

**T3 · 顶栏按钮集不按面收**（控件类）
- 位置：Figma topbar 组件集有 `actions=full` / `actions=export` 两变体：`04/05/08` 帧动作区只有 `导出` + `已保存`；编辑器 [app.ts:1063-1068](../../../preCompute/FishHabitEditor-0.3.4.0-b/src/ui/app.ts) 只按 isBass 禁用预设，Reset/导出/Bass 预设在**所有面**都显示。
- 差异：预览面与列表面多了 Reset 与 Bass 预设（Bass 预设在 resolve/bake 面无意义）。
- 量级：**控件级** ｜ 可开工度：**现在就能改**（按 `nav` 收按钮集）。

**T4 · 顶栏几何**（布局类）
- 位置：Figma topbar 实例 `1640x54` @ y=16，主内容 y=84 起（各帧一致）；编辑器 [index.html · `header.hdr` 规则](../../../preCompute/FishHabitEditor-0.3.4.0-b/index.html)——基线 `height: 64px` 通栏、main 紧随其后；@ master `23cf6e2` 该规则已为 `height: 54px`（＝图值，「64 vs 54」半已过期；内容起点半未复核）。
- 差异：顶栏高 64 vs 54、内容起点 64 vs 84（含 16px 上边距层）。
- 量级：**样式级** ｜ 可开工度：**现在就能改**（建议与设计确认一次再动）。

**T5 · 顶栏副标题（hdr-sub）为图无元素**（文案类）
- 位置：编辑器 [app.ts · `hdrSub` 设置点](../../../preCompute/FishHabitEditor-0.3.4.0-b/src/ui/app.ts) `$('hdrSub')` 各面文案（如 '底板在 species 层编辑；engagement mode（≙ FishEnvAffinity 行）在其上做 override / 微调'）；Figma topbar 实例内**无副标题文本节点**。
- 量级：**文案级**（编辑器超集） ｜ 可开工度：**待 Owner 裁决**（去留；若留则不算 delta）。

### 左栏 drawer

**D1 · 覆盖层 label 文案**（文案类）
- 位置：Figma drawer 覆盖层 label（**'兼容壳 · 覆盖层'**）；编辑器 [rail.ts:119](../../../preCompute/FishHabitEditor-0.3.4.0-b/src/ui/render/rail.ts) `'ENGAGEMENT MODE · 覆盖层'`。
- 差异：图已从 mode 措辞改为「兼容壳」措辞（并配 dirtyDot），编辑器还是旧词。
- 量级：**文案级** ｜ 可开工度：**现在就能改**。

**D2 · 覆盖层行形态（三行 vs 单行 mode 壳）**（状态类）
- 位置：Figma 物种 drawer 只有一行 'NORMAL · 1 处覆盖' + label 行旁 `dirtyDot`（6×6 圆点）；编辑器 [rail.ts · `ENGAGEMENT_KEYS` 三行渲染](../../../preCompute/FishHabitEditor-0.3.4.0-b/src/ui/render/rail.ts) 列 `Species Base` / `幼年` / `成年及以上` 三行 + 每行右侧「未存」小徽标。
- 差异：图是「兼容壳 mode」单行壳（NORMAL 是兼容壳 mode 名，编辑器模型无此 mode）；编辑器按本版模型列三层。
- 量级：**结构性** ｜ 可开工度：**待 Owner 裁决**——这是 rail.ts「兼容壳 · 覆盖层」注释里的**已裁有意偏差**（Owner 2026-09-18 裁每组 species 只留两个 engagement group，不照抄假图 mode 名）。图演进成「兼容壳」后两边口径要不要合，需 Owner 定，**不按图直接改**。

**D3 · 模板工作区组件清单的字面**（文案类）
- 位置：Figma drawer 组件清单行 **'觅食水层 · FEEDING LAYER'**、**'时段 · TIME_PERIOD'**；编辑器 [template.ts:123](../../../preCompute/FishHabitEditor-0.3.4.0-b/src/ui/render/template.ts) 直接拼枚举值 ⇒ '觅食水层 · FEEDING_LAYER'、'时段 · TIME_PERIOD'。
- 差异：空格 vs 下划线（逐字）。
- 量级：**文案级** ｜ 可开工度：**现在就能改**（显示层做一次 `replace('_',' ')`，别动枚举）。

**D4 · 模板工作区缺「空间机会策略 · POLICY」行；多「品质 · FishQuality」行**（控件类）
- 位置：Figma drawer 组件清单 **'空间机会策略 · POLICY'** 行；编辑器 [template.ts:133](../../../preCompute/FishHabitEditor-0.3.4.0-b/src/ui/render/template.ts) 无 POLICY 行，反有一行 off 的 '品质 · FishQuality'（图无）。
- 差异：组件清单第五项错位——「空间机会策略 · POLICY」图有（可点入口）、编辑器无；「品质 · FishQuality」编辑器有（置灰）而图无。
- 量级：**控件级** ｜ 可开工度：**拆两半**——POLICY 行：**壳现在就能改**（补禁用/待接行），点进去的 POLICY 模板编辑面**依赖后端**（策略模板清单/编辑模型未实现，编辑器现只有 `state.rules` 行级 Role patch）；品质行：`FISH QUALITY STABLE` 本版继续置灰，编辑器现状符合边界，**不按图删除或开放该入口**。

**D5 · 鱼列表 FILTER 行文案**（文案类）
- 位置：Figma（08）FILTER 行 **'按钓场收窄 · 待定'**；编辑器 [rail.ts:76](../../../preCompute/FishHabitEditor-0.3.4.0-b/src/ui/render/rail.ts) '按钓场收窄'（disabled，title 另有说明）。
- 量级：**文案级** ｜ 可开工度：**现在就能改**。

**D6 · 物种 drawer 名下副行为图无**（文案类）— 编辑器 [rail.ts · `rail-sub` 副行](../../../preCompute/FishHabitEditor-0.3.4.0-b/src/ui/render/rail.ts) `scientific_name · name` 副行；图 drawer 只有物种名。**待裁决**（超集，对本版作者有用）。

**D7 · railfoot 多一行**（文案类）— 编辑器物种面 railfoot 多 'No BakeProgramRef / DSL'（[rail.ts · `railfoot`（No Routing / Share 行）](../../../preCompute/FishHabitEditor-0.3.4.0-b/src/ui/render/rail.ts)）；图只有 'No Routing / Share'。**待裁决**（超集）。

### 第三栏 contextualEditor（460）

**F1 · 非作者面的第三栏空态壳缺失**（布局类）
- 位置：Figma 每帧第三栏都有 titleRow（`←` + 面名 + `关闭 ×`）+ emptyHint：04 标题 **'来源链'** / hint **'选中左侧任意一行，查看它的来源链'**；05 标题 **'条件详情'** / '选中左侧任意一个条件，查看它的求值链'；07 标题 **'模板编辑'** / '选中左侧任意一个模板开始编辑'；08 标题 **'鱼摘要'** / '选中左侧任意一条鱼，查看它的概览'。编辑器：预览/列表面不渲染第三栏（`#focusCol` **残留上一个作者面的内容**，[app.ts:763-794](../../../preCompute/FishHabitEditor-0.3.4.0-b/src/ui/app.ts) 的 render 各分支不碰它）；模板工作区 `#focusColTpl` 恒空。
- 差异：四面各缺「标题行 + 逐字 hint」的空态壳；且预览面有**内容残留**问题（切到 Resolve 后第三栏还显示作者面的编辑卡）。
- 量级：**结构性** ｜ 可开工度：**现在就能改**（壳 + 文案先落；选中行后的详情内容另见 C 类依赖）。

**F2 · 作者面空态（未选组件时）的标题行与 hint**（控件/文案类）
- 位置：Figma（01）空态 contextualEditor：titleRow（`←` / **'焦点编辑'** / `关闭 ×`）+ hint **'点左侧任一组件卡开始编辑'**；编辑器基线只有 CSS 空态文案（'焦点编辑栏 · 选中一个组件卡后，逐字段编辑在这里（本切片只落外壳）'，无 titleRow）——@ master `23cf6e2`（2026-09-23）已落：titleRow（`←`／`焦点编辑`／`关闭 ×`）＋ 逐字 hint「点左侧任一组件卡开始编辑」（[focus.ts · 作者面空态](../../../preCompute/FishHabitEditor-0.3.4.0-b/src/ui/render/authoring/focus.ts) :51,:61 真渲染）。
- 量级：**控件级**（titleRow 缺）+ 文案级（hint 逐字） ｜ 可开工度：**已落**（@ `23cf6e2`，锚见位置行）。

**F3 · 展开态标题文案**（文案类）
- 位置：Figma（10）**'← 水温习性'**、（11）**'← 结构体亲和'**、右侧 **'关闭 ×'**（蓝 `#2e63b8`）；编辑器 [focus.ts:57-61](../../../preCompute/FishHabitEditor-0.3.4.0-b/src/ui/render/authoring/focus.ts) `'水温习性 · 编辑面'` + 按钮 **'收起 ^'**。
- 差异：返回语义（← 组件名）与关闭控件（关闭 ×）vs 「· 编辑面」+「收起 ^」。
- 量级：**文案级** ｜ 可开工度：**现在就能改**。

---

## 一、帧（01 Affinity Compat Mode Authoring）

**A1 · 首行选择器 label**（文案类）
- 位置：Figma 首行 label（**'来源 / Source'**，10px/600）；编辑器 [panels.ts:74](../../../preCompute/FishHabitEditor-0.3.4.0-b/src/ui/render/authoring/panels.ts) `<span class="tlab b">模板</span>`。
- 量级：**文案级** ｜ 可开工度：**现在就能改**。

**A2 · 「档案级字段」块缺失（fail_env_coeff）**（控件类）
- 位置：Figma archiveLevelBlock：标题 **'档案级字段 · 与角色同层（不挂在组件卡上）'** + 字段（label **'fail_env_coeff（档案级）'**、输入 '0.01'、rangeHint **'[0, 0.10]'**、状态 '继承'）。11 帧把同一字段挪进 SpatialOpportunityPolicy 块（FailEnvCoeffCell，值 '0.01   [0, 0.10]'）。编辑器基线（2026-09-22）两处都无；@ master `23cf6e2`（2026-09-23）UI 块仍未落（`FailEnvCoeffCell` 全仓 0 命中），持久层读写通路已解（见可开工度行）。
- 量级：**控件级** ｜ 可开工度：**后端依赖已解，剩 UI 块＝前端活**（2026-09-23 读行 @ master `23cf6e2`）——读写通路实码俱在：写侧 `persistence.ts` `CoeffPatchEntry`（:407）／顶层 `coeffRules`（:559）＋逐条校验（:676）、`app.ts` `failEnvCoeffIntoEntry`（:83,:2479）；读侧 `app.ts` `setFailEnvCoeff(entry.policyRecipe?.failEnvCoeff)`（:748）；物种层／行级两条 lane 分立是设计而非缺失。`FailEnvCoeffCell` UI 块待落（主代理 2026-09-23：已认领、与 S1 同批）。

**A3 · 兼容壳「当前关联 Quality」只读摘要缺失**（布局类）
- 目标：习性档案 / Compat Mode 上下文只读列出当前 Production / 数据迁移映射到该 `FishEnvAffinityRef` 的已有 Quality，用于说明这份习性会影响哪些品质。
- 差异：Figma 现有块仍带下拉、`✕` 与「+ 添加品质」等可编暗示；编辑器当前没有这块。目标态不提供添加、删除、重分配、占比或 Routing 编辑，也不显示 `100%` 作为未来 Mode Share 的替代语义。
- 量级：**结构性**（只读摘要块） ｜ 可开工度：**现在就能改**——编辑器补只读摘要；Figma 去掉 Quality mutation 控件。Quality 自身字段仍由置灰的 `FISH QUALITY STABLE` 边界关闭。
- 附带（同一区域）：占比 / 比例与分群逻辑的 disabled 占位见 **A4**。

**A4 · disabled 控件形态**（文案/样式类）
- 位置：Figma disabledControls：两枚 250×30 控件框，框内逐字 **'占比 / 比例 · disabled（不可编辑）'**、**'分群逻辑 · disabled（已关闭）'**；编辑器把这两句放进了 note-card 正文（[notes.ts · disabled 两句文案](../../../preCompute/FishHabitEditor-0.3.4.0-b/src/ui/render/authoring/notes.ts)，一行文字，非控件框）。
- 差异：图是**两枚置灰控件框**，编辑器是 note-card 里的一行引用文字。契约明文要旨：占比/比例与分群逻辑两处**只以禁用占位行呈现（字段位在、控件不在），不提供编辑控件** ⇒ **编辑器现状（占位行）反而更近契约形**，向图的「控件框」对齐方向与契约相反。
- 量级：**样式级** ｜ 可开工度：**待 Owner 裁决（canvas ↔ 契约相抵）**——偏差侧在图，不在编辑器；文案本身两侧一致。

**A5 · Authoring boundary 第三行**（文案类）
- 位置：Figma（01）boundary 正文逐字 **'No FishQuality environmental Profile partial override.\nNo FishQuality Role override.\nNo Routing / Share.'**；编辑器 [notes.ts:40-43](../../../preCompute/FishHabitEditor-0.3.4.0-b/src/ui/render/authoring/notes.ts) 第三行为 **'No BakeProgramRef · RequiredParamSchema · DSL · Routing · Share.'**
- 量级：**文案级** ｜ 可开工度：**现在就能改**（按图收窄第三行）。

**A6 · 列底状态行形态（收起摘要行 vs 常开 box 列表）**（布局类）
- 位置：Figma 各作者面帧列底：1px 分隔线 + **单行两枚摘要**：**'持久态孤儿 2 行 ▸'**（琥珀 `#9a6b1f`）+ **'生产配置外部变化 · 只读诊断 ▸'**（蓝）+ provenance 行 **'Editor State = Authoring Truth · Publish → Production · Bootstrap once'**（11 帧已改中文形：**'Authoring Truth：Editor State · Production：Publish materialize · Bootstrap 仅初始化一次'**）。编辑器 [state.ts:40-89](../../../preCompute/FishHabitEditor-0.3.4.0-b/src/ui/render/state.ts)：一行 '编辑器持久化（本地文件 + Git）· 已存 N 个鱼种 · 原样复用 X / 改过 Y…' + 常开 warnbox/errbox 列表。
- 差异：图是「两条可展开（▸）诊断摘要 + provenance 行」；编辑器是统计行 + 常开 box。state.ts 自己注明「内容与 §9.2 的『只读诊断一道口子』不符——另一条工作项」。
- 量级：**结构性** ｜ 可开工度：**现在就能改**（形态 + 两代文案取 11 帧中文形；展开态交互属卡6 目标态，可分步）。

**A7 · 组件卡 LocalState 徽标在底板 scope 缺失**（状态类）
- 位置：Figma（01）底板帧卡上即有四枚 LocalState 徽标：**'1 处覆盖 · 核心条件' / '继承 · 核心条件' / '继承 · 次要条件' / '继承 · 不消费'**（01 帧长形）；（10）/（11）为短形 **'1 处覆盖' / '继承'**。编辑器 [componentCard.ts:68-70](../../../preCompute/FishHabitEditor-0.3.4.0-b/src/ui/render/authoring/componentCard.ts)：`ctx.inGroup` 才出徽标，**底板 scope 卡上无任何 LocalState**。
- 量级：**控件级**（状态显示缺失） ｜ 可开工度：**现在就能改**（底板态补短形徽标；01 帧的「· 核心条件」后缀与 10/11 短形冲突，建议取短形——由 Owner 在评审时定）。

**A8 · 「编辑中」标记**（文案/状态类）
- 位置：Figma（10）卡名 **'水温习性 · 编辑中 →'**、（11）结构卡 **'结构体亲和 · 编辑中'**；编辑器卡名不带编辑中（以 `.open` 蓝框表达选中态，[componentCard.ts:75](../../../preCompute/FishHabitEditor-0.3.4.0-b/src/ui/render/authoring/componentCard.ts)）。
- 量级：**文案级** ｜ 可开工度：**现在就能改**（open 态在卡名上拼 ' · 编辑中'）。

**A9 · 校验面板位置与形态**（布局类）
- 位置：Figma Current 帧主列**无常开校验卡**——校验以列底摘要行 + 展开态存在（卡6 projection：态2 '收起·摘要行'、态3 WARNING 行、态4 ERROR 行、态8 行定位 ⚑UNIMPL）；编辑器 [authoring.ts:92](../../../preCompute/FishHabitEditor-0.3.4.0-b/src/ui/render/authoring.ts) 把 `validationCardHtml` 常开挂在主列**上方**（模板行之后）。
- 差异：常开卡 vs 列底可展开摘要。
- 量级：**结构性** ｜ 可开工度：**行定位半已落（读行依据，2026-09-23，非裁定）**——`firstLocatableError`（`src/editor/diagnostics.ts`）＋ `locateField`/`located`（`src/ui/app.ts`）已落，`tests/card6-badge-locate.test.ts` 有「删定位句 ⇒ 组六红」变异（主代理读行结论，本笔 @ `23cf6e2` 复证命中）；其余部分仍可开工（卡6 目标态不在 #59–63 射程；行级字段 sev/owner/field/msg/act 见卡6 投影态3，可分步落）。另三小项〔列底固定／二还是三／观感〕经查不在底稿 scope ⇒ 单列、不挂回本条。

**A10 · 主列超集块族（图无、编辑器有）**（布局类）
- 位置：编辑器主列还有：'运行时边界与不变量' + '已作废状态对照' note-cards（[notes.ts · `runtimeNotesHtml`（「运行时边界与不变量」＋「已作废状态对照」）](../../../preCompute/FishHabitEditor-0.3.4.0-b/src/ui/render/authoring/notes.ts)）、'本层引用的模板'（notes.ts · `templateRefsHtml`）、'数据来源（本条鱼）' details + '本编辑器不可编辑' 清单（notes.ts · `dataProvenanceHtml`）、'档案共享 ED-11①' 面板（[fishList.ts · 档案共享 ED-11① 面板（`#usage`）](../../../preCompute/FishHabitEditor-0.3.4.0-b/src/ui/render/fishList.ts)，渲染于 `#usage`）、页头 provLine（fixture 溯源行）。Current 帧主列均未画这些块。
- 量级：**结构性**（超集） ｜ 可开工度：**已裁：留（现址保持）**——高参 2026-09-23 直问裁定：9 组**全部留**，**不删、不搬、不新增控件**（逐块与登记口径见附录「高参裁定（直问版）」）。多为编辑器自加的说明面/裁定产物（ED-11① 是强制展示、`sealed` 清单是边界载体）。

---

## 二、帧（10 编辑·水温）

（与 01 帧共通条目见上节；以下为本帧特有。）

**B1 · 第三栏字段网格：四列 vs 两列**（布局/样式类）
- 位置：Figma（10）Contextual Editor 字段两列排：fieldsRow（temp_accept_min 207×2）、fieldsRow2（temp_fav_max ×2）、rowB（temp_threshold ×2），行序 accept_min/fav_min → fav_max/accept_max → threshold/falloff；编辑器基线 `.flds { grid-template-columns: repeat(4,1fr) }`（五数值 + falloff 挤四列）——@ master `23cf6e2` 该规则已改 `repeat(2,1fr)` 两列（[index.html · `.flds` 网格规则](../../../preCompute/FishHabitEditor-0.3.4.0-b/index.html) :349，「四列」半已过期；行控件形态未逐项核）。
- 量级：**样式级**（栏内布局） ｜ 可开工度：**现在就能改**（焦点栏宽 424 ≈ 图 426，两列刚好）。

**B2 · 水温卡值摘要尾巴**（文案类）
- 位置：Figma（10）（同 01 帧）**'24–29℃ · 可接受 2–35℃'**；编辑器 [componentCard.ts:28](../../../preCompute/FishHabitEditor-0.3.4.0-b/src/ui/render/authoring/componentCard.ts) 摘要拼到 `'24–29℃ · 可接受 2–35℃ · 阈值 0.05 · SMOOTHSTEP'`。
- 差异：编辑器多 ' · 阈值 X · FALLOFF' 两段。
- 量级：**文案级** ｜ 可开工度：**现在就能改**（按图截到'可接受'段）。

**B3 · 「从钓鱼元素周期表导入」形态**（控件类）
- 位置：Figma（10）第三栏底部一行蓝色链接文字 **'从钓鱼元素周期表导入'**（`#2e63b8`，426 宽整行）；编辑器基线（2026-09-22，[focus.ts · 导入入口](../../../preCompute/FishHabitEditor-0.3.4.0-b/src/ui/render/authoring/focus.ts) :68-73）＝正文 + **'未接'** 徽标 + 三句常驻说明（主代理 2026-09-21 立的「显式未接态」边界）；`961fad3`（2026-09-23）已改回可点入口形态（`focus.ts:80`，源码自标 B3／#60／卡5·卡8 落定后）。
- 差异：图=可点链接形态；编辑器=未接通告。两侧语义差在「入口是否存在」。
- 量级：**控件级** ｜ 可开工度：**已落**（`961fad3`，2026-09-23；#60 已独立 live PASS，卡5／卡8 落定形态已执行）。

> 已对齐不报：五字段集与键名（`temp_accept_min` 等逐字）、'继承' / **'已覆盖 · ↩ 恢复为底板'** 状态行逐字（[primitives.ts:170](../../../preCompute/FishHabitEditor-0.3.4.0-b/src/ui/render/authoring/primitives.ts) vs（10））、曲线预览（梯形+阈值线+四温度标注，[temperature.ts:41-77](../../../preCompute/FishHabitEditor-0.3.4.0-b/src/ui/render/authoring/temperature.ts) vs（10）curvePreview）、falloff 下拉。

---

## 三、帧（11 编辑·结构）——最新代形态

**S1 · SpatialOpportunityPolicy 块 vs Calculation Surface 卡**（布局/控件类）
- 位置：Figma（11）SpatialOpportunityPolicy 块（820×92）：标题 **'空间机会聚合策略'** + 右侧 **'策略模板 · 当前绑定'** + 一行四格 RoleCell（**'水温 · Temperature'** CORE▾ / **'结构 · Structure'** CORE▾ / **'觅食水层 · Feeding'** SECONDARY▾ / **'时段 · Time Period'** IGNORED▾，roleBadge 带 ▾ = 下拉）+ FailEnvCoeffCell（见 A2）。编辑器 [authoring.ts:108-112](../../../preCompute/FishHabitEditor-0.3.4.0-b/src/ui/render/authoring.ts) + [panels.ts:120-130](../../../preCompute/FishHabitEditor-0.3.4.0-b/src/ui/render/authoring/panels.ts)：标题 **'Calculation Surface · Spatial Opportunity'** + 'AggregationRole' pill + **四行** `CONDITION → select → note` 表。
- 差异：标题文案、组织（一行四格 policy 卡 vs 四行表）、Role 控件形态（badge▾ vs 裸 select）。
- 量级：**结构性** ｜ 可开工度：**UI 块已认领在飞**（#61 已独立 live PASS、前提消失；形态半已落／整块未落读数见 §十二 注；主代理 2026-09-23：整块 UI 已认领、与 A2-UI 同批在飞。原 #61 目标态＝RoleControl 两入口同一真值，已随其定稿）。

**S2 · roleEntry 展开态行缺失**（控件类）
- 位置：Figma（11）的 roleEntry 展开态行：**'水温 · Temperature 的 Role'** ｜ **'物种层'** ｜ **'Role 已钉住'** ｜ **'仅使用来源'** / **'设置为'** ｜ **'本行覆盖'** ｜ **'沿用物种操作'** ｜ **'仅使用当前来源'** ｜ **'设置为'** ｜ **'目标行：LAKE_A × LARGEMOUTH_BASS/Q3'**（含两条 1px 竖分隔）。编辑器无任何对应展开态。
- 量级：**控件级** ｜ 可开工度：**高参裁定收口（2026-09-23，经开发线直投）——已落部分保持不动；余量登记缺口**（明令：不自造 Role 词表、不把值侧操作词迁移到 Role、不把目标行归属搬进该行；原 #61 的 INHERIT 与同原始值 SET 区分随其定稿）。

**S3 · 组件卡上 Source 选择器 + 「物种层」标签**（控件/状态类）
- 位置：Figma（11）：四张 `ComponentCard` 实例上叠四枚 Source 选择器（`sel_来源__temperature/structure/feedingLayer/timePeriod`，值：'Temp_Largemouth_Bass  ▾' 等）+ 四枚 **'物种层'** 层级标签（`roleLayerTag_*`）。编辑器卡上模板行为**只读**（[componentCard.ts:81](../../../preCompute/FishHabitEditor-0.3.4.0-b/src/ui/render/authoring/componentCard.ts) '模板 ${rowName}'，title「只读」）；层级标签无。
- 差异：卡上 Source 是「双入口」之一（§1：前层选择器 ↔ 卡上 Source 下拉）；编辑器只落了前层入口。
- 量级：**控件级** ｜ 可开工度：**依赖 #60**（F2.4 涉「来源」修订；卡1 七态投影在 PROJECTION A① 区，态2/3/4 的 pin/rebase 细节会随之定）。数据面 `onSwitchTemplate` 已有。

**S4 · 结构第三栏：五列网格 vs 两列行列表**（布局类）
- 位置：Figma 第三栏 structureList2Col（11）：**两列** × 13 行 FieldValueControl（207×44：FieldName / EffectiveValue 数值框 / TierLabel `Custom ▾`/`SUBOPTIMAL ▾`/`PREFERRED ▾`/`REJECT ▾` / LocalOp / Diagnostic）；编辑器基线 `.grid25` **五列** cell25（每格 ~80px 宽，25 格挤第三栏）——@ master `23cf6e2` 该规则已改 `repeat(2,1fr)` 两列（[index.html · `.grid25` 网格规则](../../../preCompute/FishHabitEditor-0.3.4.0-b/index.html) :273，「五列」半已过期；主代理 2026-09-23 行级核＝S4 已落）。
- 量级：**结构性**（栏内布局） ｜ 可开工度：**现在就能改**（grid 改两列行列表；第三栏 424 宽与图 426 吻合）。

**S5 · 结构摘要行缺失**（文案类）
- 位置：Figma（11）structureSummary **'25 项 · 3 个非零 · 22 个 0 · 当前均继承'**（12px/600）；编辑器结构编辑面无统计行。
- 量级：**文案级** ｜ 可开工度：**现在就能改**（25 项计数在手）。

**S6 · 字段名形态**（文案类）
- 位置：Figma FieldName 只有类型名（**'开放水域'**）；编辑器 [structure.ts:28](../../../preCompute/FishHabitEditor-0.3.4.0-b/src/ui/render/authoring/structure.ts) `${st} ${structLabel}`（'0 开放水域'）。
- 量级：**文案级** ｜ 可开工度：**现在就能改**。

**S7 · op 词表与图的两层词表**（文案类）
- 位置：Figma 卡2 投影态9 定了两层词表：物种层 **'仅使用来源' / '调整为…' / '设置为…'**；桶层缺省支按实际继承到的操作给具体串（**'沿用物种调整' / '沿用物种设置为' / '仅使用当前来源'**），态10 展开态有 '当前来源/被继承的操作/本层操作/Effective/provenance' 五行。编辑器 op select 选项（[primitives.ts:88-94](../../../preCompute/FishHabitEditor-0.3.4.0-b/src/ui/render/authoring/primitives.ts)）：**'跟随' / '调整' / '设置为' / '恢复为来源值'**，不分层。图上每格常显 LocalOp 标签 '仅使用来源'。
- 量级：**文案级**（词表逐字 + 两层分化） ｜ 可开工度：**现在就能改**（卡2 目标态已画且不在 #59–63 射程；注意缺省支「具体串」需知道实际继承到哪一支——数据在 `baseOps`，已传入渲染）。

**S8 · 每格 Diagnostic 位缺失**（控件类）
- 位置：Figma FieldValueControl 每格有 Diagnostic 文本位（当前空格占位）；编辑器 cell25 无该位。
- 量级：**控件级**（占位结构） ｜ 可开工度：**现在就能改**。

---

## 四、帧（04 Resolve Preview）

**R1 · 副标题文案**（文案类）
- 位置：Figma（04）**'Read-only compiled truth · Runtime identity stays FishQualityRef'**；编辑器 [app.ts:772](../../../preCompute/FishHabitEditor-0.3.4.0-b/src/ui/app.ts) '只读的编译结果 · 运行时 identity 仍是 FishQualityRef'（中文转写）。
- 量级：**文案级** ｜ 可开工度：**现在就能改**（以图英文原文为准与否，一次定）。

**R2 · ResolvedSpatialOpportunitySubject 行族**（文案类）
- 位置：Figma（04）逐字：`fishQualityRef   LARGEMOUTH_BASS / Q3` ／ `profile source    FishEnvAffinityRef = NORMAL` ／ `AggregationRole source   NORMAL · 条件角色（加在 \`FishEnvAffinity\` 上的列）` ／ `Runtime Mode identity   — none` ／ `Effective: …`。编辑器 [preview.ts:94-101](../../../preCompute/FishHabitEditor-0.3.4.0-b/src/ui/render/preview.ts)：`speciesId` + `fishQualityId` 两行（vs 图单行 `fishQualityRef`）、profile source 值无 **'= NORMAL'**、AggregationRole source 值无 **'NORMAL ·'** 前缀。另编辑器多一张 noteCard「本块的来源」（`preview.ts:112-117`，图无）。
- 量级：**文案级** ｜ 可开工度：**现在就能改**（键名/行合并直接改）；⚠️ **'= NORMAL' 一段半依赖模型**：编辑器 scope 是 base/young/mature，无「兼容壳 mode 名 NORMAL」的显示映射——补这半句需要先把 scope→兼容壳 mode 名的口径定下来（与 D2 同源）。

**R3 · Subject provenance 行值**（文案类）
- 位置：Figma（04）：`Temperature Profile ← FishEnvAffinityRef NORMAL`×3 + `AggregationRole ← 同一 NORMAL 行（条件角色是加在 \`FishEnvAffinity\` 上的列）`；编辑器 [preview.ts:122-123](../../../preCompute/FishHabitEditor-0.3.4.0-b/src/ui/render/preview.ts)：值 'FishEnvAffinityRef'（无 NORMAL）+ '同一 FishEnvAffinity 上的条件角色列'（措辞不同）。
- 量级：**文案级** ｜ 可开工度：同 R2（NORMAL 半依赖）。

**R4 · ResolvedOpportunitySeed：键名与 Source row 行**（文案类）
- 位置：Figma（04）：`fishPondRef       LAKE_A` ／ **`fishQualityRef    LARGEMOUTH_BASS / Q3`** ／ BaseOpportunityIntensity ／ isBackgroundFish ／ **`Source row: LAKE_A × Q3`**。编辑器 [preview.ts:126-133](../../../preCompute/FishHabitEditor-0.3.4.0-b/src/ui/render/preview.ts)：键 **'fishQualityId'**、无 `Source row:` 行（数据在手：fishPondRef × fishQualityRef 可拼）。
- 量级：**文案级** ｜ 可开工度：**现在就能改**。

**R5 · 编辑器超集**（量级：**结构性**；**已裁：留（现址保持）**）：Boundary 块（[preview.ts · `noteCard` / `warnHtml` / Boundary 块](../../../preCompute/FishHabitEditor-0.3.4.0-b/src/ui/render/preview.ts)）、WARNING/ERROR box 区（warnHtml）、noteCard。图无。★ 高参 2026-09-23 直问裁定：**全留、现址保持**；两个 noteCard **不得合并**成一个泛化说明块、各留当前语义所属页面（见附录）。

---

## 五、帧（05 Bake Preview）

**K1 · 副标题**：编辑器 pvSub '输入是条件等价类，不是空间支撑 identity'（[app.ts · `pvSub`](../../../preCompute/FishHabitEditor-0.3.4.0-b/src/ui/app.ts)）；Figma 05 帧**无副标题**。文案级，**待裁决**（超集）。

**K2 · ConditionGroup 块键值文案**（文案类）
- 位置：Figma（05）逐字：`Structures[]          [GRASS_EDGE]` ／ `TemperatureRange       [25,27]  → representativeTemp 26℃` ／ `FeedingEcologyLayer[]  [MIDDLE, BOTTOM]` ／ `TimePeriod             MORNING`。编辑器 [preview.ts:170-173](../../../preCompute/FishHabitEditor-0.3.4.0-b/src/ui/render/preview.ts)：键 'StructureType'（值=中文类型名 join ' + '）、'→ 代表温度 26℃'（中文）。
- 量级：**文案级** ｜ 可开工度：**现在就能改**（键名带 `[]`、值取类型名、'representativeTemp' 逐字）。

**K3 · 场景选择器（scenarioSel）为图无控件**：编辑器 [preview.ts · `scenarioSel`](../../../preCompute/FishHabitEditor-0.3.4.0-b/src/ui/render/preview.ts)（Bake 页唯一控件，注释声明有意保留）；Figma 05 帧无。控件级，**待裁决**（功能性超集，删则 Bake 页不可换条件组）。

**K4 · Component evaluation 行文案**（文案类）
- 位置：Figma（05）TIME_PERIOD 行 **`AggregationRole IGNORED → not consumed by Spatial Opportunity`**；编辑器 [preview.ts:179-183](../../../preCompute/FishHabitEditor-0.3.4.0-b/src/ui/render/preview.ts) 同态显示 **'不消费'**。
- 量级：**文案级** ｜ 可开工度：**现在就能改**。

**K5 · trace 数据行缺失（三中间量 + 次聚合形态）**（控件/数据类）
- 位置：Figma（05）首行 **'CoreProduct 1.000 · SecondaryProduct 1.000 · SecondaryFactor 1.000'**；（05）FEEDING_LAYER 行 **'max(1.00,0.60)=1.00'** 形态。编辑器基线（2026-09-22，bakeBlocks @ [preview.ts · trace 行](../../../preCompute/FishHabitEditor-0.3.4.0-b/src/ui/render/preview.ts) :203-209）只有 RawEnvCoeff / Gate failure branch / Background floor / FinalEnvCoeff，无三中间量行，Fit 只显 rawFit 数值；@ master `23cf6e2`（2026-09-23）三中间量与次要聚合**现行形**已渲染（`preview.ts:211-223`，源码自注明 `max(…)` 是已被取代的旧形）。
- 量级：**控件级**（缺数据行） ｜ 可开工度：**已落**（2026-09-23 读行 @ master `23cf6e2`）——`PreviewModel` 已暴露三中间量（`session.ts:50-52`）并真渲染（`preview.ts:215-216`）；次要聚合**现行形** `1 − (1 − SecondaryProduct)/4` 已渲染（`preview.ts:223`）。（05）FEEDING_LAYER 行图示的 `max(1.00,0.60)=1.00` 是 2026-09-17 改形前的已废旧形，勿按旧形回写。

**K6 · Canonical output 分隔线**：Figma（05）有 `────────────────────────────` 分隔行；编辑器无。样式级，**现在就能改**。

**K7 · ConditionGroup 标题**：Figma（05）**'ConditionGroup · CG-01'**；编辑器 'ConditionGroup'（无编号）。文案级，**现在就能改**（CIG 编号 session 侧有 `cig01Note`，可带出）。

---

## 六、帧（07 Component Template Workspace）

**P1 · tplSub 副标题**：编辑器 [index.html · `tplSub`](../../../preCompute/FishHabitEditor-0.3.4.0-b/index.html) '入口是组件视图里的一行小字「提取为模板」…'；Figma 07 帧主列无副标题。文案级，**待裁决**（超集；这句话本身是（07）帧副标题的历史转写）。

**P2 · 模板清单行形态**（样式类）
- 位置：Figma 清单行文案形态 **'struct_tench · 丁鱥'**（模板名 · 中文名）+ 右侧滚动条；编辑器基线（2026-09-22，[template.ts · 模板清单行](../../../preCompute/FishHabitEditor-0.3.4.0-b/src/ui/render/template.ts) :161-174）行 = 名字+id 两行 + 计数两列 + 归档标记格；`961fad3`（2026-09-23）已改单行「模板名 · 中文别名」（`template.ts:426`，源码自标 P2／卡8 定稿）。
- 量级：**样式级**（行版式） ｜ 可开工度：**已落**（`961fad3`，2026-09-23；#60 已独立 live PASS，卡8 定稿形态已执行）。

**P3 · 「共享影响」常驻摘要块缺失**（布局类）
- 位置：Figma 共享影响块：标题 **'共享影响 · 共 7 条鱼 / 7 个鱼种（当前直接引用）'** + 引用名单滚动块。编辑器基线（2026-09-22）无此常驻块（引用者信息在**选中某行后**的详情卡里）；`961fad3`（2026-09-23）已落常驻块（`template.ts` `renderSharedImpact`，写 `$('sharedImpact')` :175，源码自标 P3／卡12 定稿；详情卡的两集分列已上移至该块）。
- 量级：**结构性**（信息组织：常驻摘要 vs 行详情） ｜ 可开工度：**已落**（`961fad3`，2026-09-23；#60 已独立 live PASS，卡12 定稿形态已执行）。

**P4 · templateImpactPreview（影响预览）块缺失**（布局类）
- 位置：Figma templateImpactPreview（854×192）：标题 **'影响预览 · Shared Template Edit'** + 右上 **'确认模板变更'**（蓝）+ '候选修改 → before / after' + 四 Metric（**'直接引用' 7 ／ '实际使用' 预览计算 ／ '最终变化' 预览计算 ／ '新增诊断' 预览计算**）+ ConsumerSummary **'当前直接引用 7 条鱼 / 7 个鱼种\n候选修改后计算：实际使用 / 最终变化 / 新增 Error·Warning\n显式确认后原子提交'**。编辑器基线（2026-09-22）无影响预览块（写回卡里的 warnbox 是另一形态）；`90a4ad1`（2026-09-23）已落（见可开工度行）。
- 量级：**结构性**（整卡） ｜ 可开工度：**已落**（`90a4ad1`，2026-09-23）——影响预览块 `renderTemplateImpact`（定义 `template.ts:224`、调点 `:140`）＋候选预览计算管线 `templateValueChange.ts`（`previewTemplateValueChange` 对候选 fixture 重 resolve；接线 `app.ts:117/:260/:1662`，渲染读 `s.templateValuePreview` @ `template.ts:236`）。原「依赖后端——当前编辑器无『对候选改动跑影响计算』的能力」前提已失效。

**P5 · 「从已有的鱼抽取模板」收起态**（样式类）
- 位置：Figma（07）该块只有一行标题（收起态 46 高）；编辑器 [template.ts:226-233](../../../preCompute/FishHabitEditor-0.3.4.0-b/src/ui/render/template.ts) 默认展开三条口径行（'默认复用…/微调派生…/就地改…'，图无这些文字）。
- 量级：**样式级**（收起/展开默认态 + 超集文字） ｜ 可开工度：**现在就能改**（口径行去留待裁，可先按图收起）。

---

## 七、帧（08 Fish List · CN / EN / ID）

**L1 · 搜索 placeholder 逐字**（文案类）
- 位置：Figma（08）搜索框 placeholder **'搜索中文名 / English / Fish ID'**；编辑器基线为 '按中文名 / 英文名 / slug 筛选'——@ master `23cf6e2`（2026-09-23）[index.html · #fishFilter 搜索框](../../../preCompute/FishHabitEditor-0.3.4.0-b/index.html) :596 placeholder 已为图逐字。
- 量级：**文案级** ｜ 可开工度：**已落**（@ `23cf6e2`）。

**L2 · 列表行右侧 engagement pills 为图无**（控件类）
- 位置：Figma 列表行为纯三列文本（中文名 15px/600 → 英文名 12px → ID 11px 等宽最浅）；编辑器 [fishList.ts · 行尾 engagement pills](../../../preCompute/FishHabitEditor-0.3.4.0-b/src/ui/render/fishList.ts) 行尾加 '幼年'/'成年及以上' pills 或 '无此规格'。
- 量级：**控件级**（超集） ｜ 可开工度：**待裁决**（三级显示口径图很纯粹；pills 是编辑器自加的导航捷径）。

**L3 · 页头副标题多句**：Figma（08）**'顺序固定：中文名 → 英文名 → ID'** 截止；编辑器基线同句后多 '。以 basic_fish_species entry 为显示单位，不按习性行铺开'——@ master `23cf6e2`（2026-09-23）[index.html · `.msub`「顺序固定」行](../../../preCompute/FishHabitEditor-0.3.4.0-b/index.html) :589 该超集句已删。文案级，**已落**。

**L4 · 「配置表目录 · 别名」块为图无**（量级：**控件级**；待裁决）：编辑器 [index.html · 「配置表目录 · 别名」块（`#srcAlias`／`#srcAliasSave`）](../../../preCompute/FishHabitEditor-0.3.4.0-b/index.html)（GAP-ED-10③ Owner 裁定加的）。**待裁决**（超集，有裁定背书）。

> 已核实不报：第二级显示内容（fixture `scientific_name` 实值即英文名，值对）；'无可编习性的条目 · 显式列出' 标题逐字一致（编辑器 [fishList.ts:96](../../../preCompute/FishHabitEditor-0.3.4.0-b/src/ui/render/fishList.ts)）；三级字号/字重/等宽形态与图一致（index.html `.frow` 族）。

---

## 八、跨帧样式

**X1 · 字号族偏差**（样式类）：主列大标题编辑器 20px（`.mh1`）vs 图 22px/600（各帧大标题）；drawer 物种名编辑器 18px（`.rail-h1`）vs 图 16px（drawer 物种名）；顶栏标题 19px vs 图 20px。**现在就能改**（一次性对齐字号表）。

---

## 九、「Figma 有、编辑器无」汇总（区域级）

任务书问 07/08 是否无对应面——**七帧都有对应面**（模板工作区与鱼列表均已存在）。真正「帧上有、编辑器完全没有」的区域：

| # | 区域 | Figma 位置 | 主条目 | 依赖 |
| --- | --- | --- | --- | --- |
| 1 | Publish 按钮 | btn_Publish（actions=full 五帧顶栏） | T1 | 高参裁定暂不实现（2026-09-23，经开发线直投） |
| 2 | 档案级 fail_env_coeff 字段 | archiveLevelBlock ／ FailEnvCoeffCell | A2 | 已解（读写通路在 @ `23cf6e2`）；剩 UI 块＝前端活 |
| 3 | 兼容壳 Quality 只读摘要 | 兼容壳 Quality 区域 | A3 | 无；按现行 Contract 补只读投影 |
| 4 | SpatialOpportunityPolicy 新形态 + roleEntry 展开态 | SpatialOpportunityPolicy ／ roleEntry 族 | S1/S2 | S1 整块已认领在飞（与 A2-UI 同批）；S2 高参裁定收口（余量缺口） |
| 5 | 卡上 Source 选择器 + 「物种层」标签 | sel_来源 选择器 ／ roleLayerTag | S3 | #60 |
| 6 | 结构第三栏摘要行 / Diagnostic 位 | structureSummary ／ FieldValueControl·Diagnostic | S5/S8 | 无（A） |
| 7 | 列底两枚诊断摘要 + provenance 行 | statusRow ／ provenanceRow（01/11 两代文案） | A6 | 无（A） |
| 8 | 非作者面第三栏空态壳（×4） | contextualEditor 空态壳（04/05/07/08） | F1 | 无（A） |
| 9 | 共享影响常驻摘要块 | 共享影响块 | P3 | **已落**（`961fad3`） |
| 10 | templateImpactPreview 影响预览块 | templateImpactPreview | P4 | **已落**（`90a4ad1`，含预览计算管线） |

## 十、已裁偏差（不按图改，留痕）

| 区域 | 现状 | 出处 |
| --- | --- | --- |
| D2 rail 覆盖层三层行 vs 图单行 mode 壳 | rail.ts:14-21 注释：Owner 2026-09-18 裁按本版模型列两组；图后来演进为「兼容壳」措辞——两边口径合流需 Owner 定 | D2（上） |
| rail 两枚置灰入口（FISH QUALITY STABLE / OPPORTUNITY · PRESENCE） | 图 01 帧旁注「入口置灰不开放」+「不要求画、不要求改」——置灰即对齐，非 delta | rail.ts:13-16 |
| `FIXED TEMPLATE` 徽标 | 图每帧右上；编辑器 index.html `.w4b` 徽标已照抄（W4B 批次标记，删否另有议论） | 不报 |

## 十一、在飞项 ↔ 本清单覆盖对照

| Issue | 射程（卡） | 命中的条目 |
| --- | --- | --- |
| #59 F2.3 自动保存与候选取消 | 卡7 | 顶栏 saveStatus 四态与「已保存」文案编辑器已落（不列 delta）；候选/取消边界属行为目标态，改完刷新 T1/T2 区域的复核 |
| #60 F2.4 来源、导入与引用列表 | 卡5/8/12 | B3（周期表导入）、S3（卡上 Source 选择器）、P2（清单行）、P3（共享影响摘要） |
| #61 F2.5 RoleControl 两入口 | 卡4 | S1（SpatialOpportunityPolicy 形态）、S2（roleEntry 展开态）、A7 的角色后缀取舍 |
| #62 F2.6a 模板完整值编辑 | 卡10/N2 | T1（Publish）、P4（影响预览） |
| #63 F2.6b Replace References | 卡11 | 当前编辑器无该面（新卡）；落定后在 07 帧补 → 暂无既有条目可标 |

## 十二、汇总

（每条恰好计一档；正文各条目处的量级标签为准。）

| 量级 | 条数 | 明细 |
| --- | --- | --- |
| **结构性** | **11** | F1, A3, A6, A9, A10, D2, R5, S1, S4, P3, P4 |
| **控件级** | **14** | T1, T3, D4, A2, A7, F2, S2, S3, S8, B3, K3, K5, L2, L4 |
| **文案级 + 样式级** | **33** | 文案 26：T2, T5, D1, D3, D5, D6, D7, A1, A5, A8, B2, F3, R1, R2, R3, R4, K1, K2, K4, K7, L1, L3, P1, S5, S6, S7；样式 7：T4, X1, A4, B1, K6, P2, P5 |
| 合计 | **58** | |

**可开工度分布**（计数为初勘口径；后续个别条目落地不回改计数，以各条目可开工度行为准——至 2026-09-23 已落：T2/F1/F2/L1/L3/B3/P2/P3/P4/K5/A9 行定位半等）：
- **不依赖任何在飞项、立刻可开工：35 条** —— 结构 4（A6, A9, S4, F1）；控件 5（T3, D4-壳, A7, F2, S8）；文案 21（T2, D1, D3, D5, A1, A5, A8, B2, F3, R1, R2※, R3※, R4, K2, K4, K7, L1, L3, S5, S6, S7）；样式 5（T4, X1, B1, K6, P5）。※ R2/R3 中 `= NORMAL` 半句另半依赖 scope→兼容壳 mode 名口径（与 D2 同源），先改键名/行合并、NORMAL 段等口径定。
- 依赖 #59–63 目标态落定：8 条（T1, S1, S2, S3, B3, P2, P3, P4）——★ **前提已于 2026-09-23 整体消失**（#57–#63 全部独立 live PASS）；8 条现状（代码读数项统一 @ preCompute master `23cf6e2`，2026-09-23 实读；落地笔 `961fad3`／`90a4ad1` 同注）：T1＝高参裁定暂不实现（2026-09-23，经开发线直投；见上）；S1＝**部分**（形态那一半已落：`policyCellsHtml` 全仓 6 处（src/ 5＋index.html 1）、`roleRowsHtml` 仅存 index.html 注释 1 处；整块未落：`FailEnvCoeffCell`／`roleEntry` 全仓 0 命中）；S2＝高参裁定收口（经开发线直投）：已落部分保持不动、余量登记缺口（读行佐证仍立：展开态行特征词 0 命中——词是画布侧命名 token，结论由「编辑器无此展开态行为」佐证）；S3＝过程性（编辑器主件在飞，已派 0.3.4.0 开发；非代码读数，无 rev 标注）；B3/P2＝**已落**（`961fad3`：`focus.ts:80` 可点入口形态／`template.ts:426` 单行「模板名 · 中文别名」，源码自标条目号；原注「画布侧已裁保留现状 ⇒ 编辑器侧无事可做」的后半句作废，「画布侧保留现状」裁定不受影响）；P3＝**已落**（`961fad3`：`renderSharedImpact` 常驻块）；P4＝**已落**（`90a4ad1`：`renderTemplateImpact` ＋ 候选预览计算管线）。
- 依赖未实现后端能力：**0 条**（2026-09-23 读行 @ master `23cf6e2`；原列 2 条前提均已失效）——A2＝读写通路实码俱在（锚见 A2 条可开工度行）；K5＝三中间量与次要聚合**现行形** `1 − (1 − SecondaryProduct)/4` 均已渲染（锚见 K5 条；（05）FEEDING_LAYER 行图示的 `max(…)` 为已废旧形，勿按旧形回写）。同句旧注「P4 含『预览计算管线』」亦已失效（`90a4ad1` 已落）；「D4 的 POLICY 编辑面部分同此」本笔未复核、暂持原状。
- 待 Owner 裁决（图无超集 / 已裁偏差 / **canvas ↔ 契约相抵**，不依赖在飞项与后端）：**10 条** —— 结构 1（D2）、控件 3（K3, L2, L4）、文案 5（T5, D6, D7, P1, K1）、样式 1（**A4**）。

---

## 附（2026-09-23）｜13 条引用锚更正 ＋ 高参裁记录 ＋ 座位留痕

**座位留痕**：本节由 #43 编排线落笔——正本 writer（清单原作者会话）已结束、座位空缺，本笔由编排线继承；**仅改引用与记录裁定，条目措辞未动**（措辞仍归原作者）。

**引用锚更正（本笔机械改动；依据＝高参裁定「正本后续修引用必须改用元素名/稳定内容锚，不能再用已漂移的行号」，**经主代理转述**）**：上方 13 条（`A10`/`R5`/`D2`/`K3`/`L2`/`L4`/`T5`/`D6`/`D7`/`P1`/`K1`/`A4`）条目内的「文件:行号」引用已全部改为**元素名/内容锚**——行号会随提交漂移。本批实读 @ `81d294f`：16 处引用中 **11 处漂移、5 处仍命中**（`rail.ts:14` 注释与 `notes.ts` 四处未漂）。全部新锚在 `81d294f` 按「文件·符号」归属解析复验命中；可跑探针形（**归属解析**，非单纯命中——本笔 `L4` 之错正是「命中≠定义处」）：于指定 rev 核「该符号/块由该文件**定义或渲染**」，如 `git grep -n "export function <符号>" <rev> -- <file>`（定义侧）并核该文件内无更高优先级的仅注释/仅调用命中；红对照＝定义侧零命中。**一处正名**：`A10` 的 `notes.ts` 引用**文件归属无误**（两节定义在 `notes.ts` 的 `runtimeNotesHtml`，经 `authoring.ts` import 于主列调用）——先前一份工作件曾判「指错文件」，经双侧逐字复核推翻，留痕防回抄。**同族第二例（本笔自犯、已回正）**：`L4` 原引 `index.html` **同样无误**（块在 `:556–567`；`app.ts` 仅有同文注释与 `saveSourceAlias` 处理函数）——本笔曾据「锚命中」误改指 `app.ts`，r2 已回正。

**高参裁定（2026-09-23，经主代理转述；高参裁≠Owner 批，Owner 另有说法按 Owner）**：
- 12 条（`A10`/`R5`/`D2`/`K3`/`L2`/`L4`/`T5`/`D6`/`D7`/`P1`/`K1`/`A4`）⇒ **「保留编辑器现有能力，画布侧补齐/更新对应设计证据」**（现有实现已核在；不应反向删代码）。

**高参裁定（直问版，2026-09-23；来源＝高参，本线**直问**、不经主代理路由；高参裁≠Owner 批，Owner 另有说法按 Owner）**：上一条是**经主代理转述**的版本（粒度粗，只说「保留现有能力」）；本批为**直问**所得，把 `A10`／`R5` **逐块**钉死 —— ★ **方向与上一条相同、更具体，不构成推翻**。
- **`A10`**（9 组：4 张 note-card（运行时边界与不变量／已作废状态对照／Authoring boundary／Engagement Mode 分群·占比 disabled）＋「本层引用的模板」＋「数据来源（本条鱼）」＋ `sealed`「本编辑器不可编辑」清单 ＋「档案共享 ED-11①」＋ 页头 provLine）⇒ **全部留、现址保持**；**不删、不搬、不新增控件**。
- **`R5`**（3 组：Boundary 块 ＋ WARNING/ERROR box 与无告警 okbox ＋ 两个 noteCard）⇒ **全部留、现址保持**；两个 noteCard **不得合并**成一个泛化说明块，各留当前语义所属页面。
- ★ 判据口径：**编辑器超集不因 Current Figma 帧未画就反向裁成错误**（Owner 已定 function-first，且本批目标不是删编辑器能力）；对这组**没有足够契约依据要求搬家** ⇒ 保持现址，**避免以「搬家」名义引入版式决定**。
- ★★ **登记口径（逐字）**：可写「**编辑器保留；Figma 侧为设计证据缺口**」；**不能**写成「实现已与 Figma 对齐」；**不能**写成「Owner 已批准视觉布局」。
- ★ 粒度＝**语义块**，**不按单个文案句拆**。
- ★ `A10` 之「Engagement Mode 分群·占比（disabled）」：留，但其**视觉暗示与 disabled 表达属产品/Owner 语义** ⇒ 本裁定**不扩写、不改成可操作控件**。
- ★ 本裁定**不决定**颜色／尺寸／排列／Figma 补图；**未来若要搬家，必须先给出明确目标容器与产品语义**（不由 Figma 缺画反推）。
- ★ **仍待 Owner/产品**（本裁定未决）：① Engagement Mode 的视觉/交互语义；② 未来若搬家的目标位。

**画布侧现状底稿**（2026-09-23T08:52Z 对拍，编排线只读；供「补齐」派工前参考）：12 条中 **10 条图无对应物**；`A4` 的 01 帧控件框已被 2026-09-23 canvas-fix 批去框（占位行＝契约形；10/11 帧未核）；**`D2` 对焦点已漂移**（drawer 组件族换代后，旧「NORMAL·1 处覆盖」单行壳与编辑器三行形均不在当前图上）⇒ `D2` 需先重建对焦点再谈补齐。

---

**附二（2026-09-23 第二笔）｜一致性收敛：已落读数 ＋ rev 标注 ＋ T1 位置锚**

本笔由 #43 编排线（分叉续体）按主代理 2026-09-23 指令执行；起因＝常驻 reviewer 对 PR #102 的同头补核：桶2 新注「B3/P2 ⇒ 编辑器侧无事可做」不成立（preCompute `961fad3` 已落），且八条「现状」注非同一 rev 读数。本笔要点（全部改前自读复证，未沿用判词转述读数）：

**性质与门史（高参 2026-09-23 复裁，经主代理转述；高参裁≠Owner 批，Owner 另有说法按 Owner）**：本笔是**纠错 PR**——对已合 #102 里「不成立的半句」与「缺失的 rev 标注」的纠错，台账／收敛性记录不足以替代。两层事实并列留痕：#102 的 `APPROVE @ 3cbca4d6` token 对该头有效；同头补核（合并前）照出的「B3/P2 半句不成立＋八注缺 rev」在合并时点**未解决**——合并已发生，不把该历史门状态改写为「当时可合」，也不回滚、不重写历史。
- **B3/P2 半句改实读形**（作者自领：原句是把「画布侧保留现状」的画布侧裁定越权推成编辑器侧结论）——`961fad3` 已落（`focus.ts:80`／`template.ts:426`，源码自标条目号）；同批 **P3 已落**（`renderSharedImpact` 常驻块）。
- **八条现状注统一标读数 rev**：代码读数项统一 @ preCompute master `23cf6e2`（2026-09-23）；`S3` 标过程性（无 rev）。
- **桶3 行归零**：A2 读写通路、K5 三中间量＋次要聚合**现行形** `1 − (1 − SecondaryProduct)/4` 均有实码（锚见各条可开工度行）；（05）FEEDING_LAYER 行图示的 `max(…)` 为已废旧形，勿按旧形回写。
- **P4 头条收敛**：旧「块缺失＋依赖后端」与 `90a4ad1`（影响预览块＋候选预览计算管线 `templateValueChange.ts`）相抵，本笔按实读收敛（条目可开工度行＋汇总表行）。
- **T1 位置行改元素锚**（`#btnReset`/`#btnExport`/`#btnPreset`；原 `index.html:478-480` 已漂至 :564/:565/:574）；另 8 处 `index.html:行号` 引用已在同日追补批转为元素锚（见下「追补」）。
- **追补（同日，主代理给出出处后）**：T1＝高参裁定暂不实现（经开发线直投，含两条明令）；S2＝高参裁定收口（已落部分不动、余量缺口，含三条明令）；A9＝行定位半已落（**读行依据、非裁定**：`firstLocatableError`／`locateField`／`located`＋`card6-badge-locate.test.ts` 变异，本笔 @ `23cf6e2` 复证命中）。
- **8 处行号锚转元素锚**（主代理同日准，#99 政策、照 T1 形）：T2/T4/F2/`.flds` 条/S4/L1/L3/§十 徽标。途中撞出的现读：**T2/F2/L1/L3 已落**（`#btnReset` 文案、空态 titleRow＋逐字 hint、`#fishFilter` placeholder、`.msub` 超集句删除）；**T4 高度半、S4 与 `.flds` 条的列数半已过期**（`header.hdr` 现 54px＝图值、`.grid25`/`.flds` 现 `repeat(2,1fr)`；各注现读，S4 另有主代理行级核＝已落）。`#publishBar` 属工作区（index.html :653），依 T1 明令不混入顶栏执行器解释。
- **仍未动**：「D4 的 POLICY 编辑面」后端依赖句（未复核）；bucket-1 其余条目（T3/D4/A7/A6/S8/F1）状态行未批量刷新（主代理 2026-09-23 已 9/9 行级核落，待另笔按其锚刷新）。
- **载体外一条**：gaps.md §③ 头注 `durableRolesOf`「看的是键在不在」锚错（实读：读值 `speciesDefault[f] !== undefined`，`persistence.ts` 全文 `hasOwn` 0 次；真正按键判的是 `rolesIntoEntry` :1605/:1610 `Object.keys(roles)`）——载体在 preCompute 仓 gaps.md（单写者＝集成线），本笔只留此指印，修复由该线落。
