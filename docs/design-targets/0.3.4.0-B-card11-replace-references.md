# 目标态离线设计稿｜A②-卡11 Replace References（批量换绑 A→B）

**这是什么**：Figma 目标态投影的**离线设计稿**。供 `#43` 编排线唯一 writer 落画；本稿零画布／GitHub／Notion 写入。

- 消费批次：`#54`（F1）／`#63`（F2.6b，**复用已落的共用N2 组**，本批只落卡11 组）。
- 契约权威：[A②-卡11](../review/ui-component-contract-r2/contract-cards.md#replace-references)；机制见[汇编 §14 两引用集](../review/ui-component-contract-r2/component-contract-consolidated.md#template-reference-sets)与[Replace 边界](../review/ui-component-contract-r2/component-contract-consolidated.md#template-replace-boundary)；staged 协议见[汇编 §8](../review/ui-component-contract-r2/component-contract-consolidated.md#source-transaction)；共用交互见[共用N2](../review/ui-component-contract-r2/contract-cards.md#rebase-impact-preview)（另册设计稿）。
- `#63` 正文要点逐字对应：「来源/目标及Direct refs-only本次写集与Effective consumers分清；保留patch、不自动pin；复用N2。」
- 画布纪律同共用N2 册首段。

---

## 0. 组的登记形态

| 项 | 值 | 依据 |
|---|---|---|
| 组（section）名 | `PROJECTION｜A②-卡11 Replace References（三态）` | 补登 figma-current.md §二「原快照未登记投影：A②-卡11」；三态沿卡9／卡12 三态组范式 |
| 帧名前缀 | `proj_rr_1` … `proj_rr_3` | 沿 `proj_*` 命名族 |
| 建议位置 | A②-卡10 组之下、同列；组宽 2460 | 列内次序 N2 → 卡10 → 卡11 |
| 建议帧尺寸 | 统一 890×250；三帧 L 形布局（2＋1，同卡9／卡12）；组高 ≈ 840 | 建议，非规范 |
| 载体 | 结构模板 A→B（ROCK 数值差可见） | 直接引用／经继承两类消费者同图可演；示例沿全画布既立集（类型名 A/B、Fish_A–C、12/31/24/2E·3W） |

---

## 1. 帧清单与逐字文案

### 态1｜选 A → 选 B（发起换绑候选）

- **验证的 interaction topology**：卡11 Actions「选 A → 选 B → 将本次换绑候选交共享N2预览／确认，提交只执行下述直接绑定 mutation」；B 侧只列合法 ACTIVE 模板（汇编 §14「归档后…不可新建引用」＋卡9 Must not「普通 Source Picker 按该节隐藏／降级归档项」；断链侧汇编 §8「禁止任何自动 fallback…必须由作者显式选新的合法来源」）。
- **帧内控件／文案**：

| 节点 | 文案（逐字） |
|---|---|
| `[st]` | `换绑 · 模板 A → 模板 B` |
| `[rs]` | `只重写 DirectReferenceSet(A) 的 durable 绑定` |
| `[a]` | `模板 A｜类型名 A（示意）· ROCK 0.40 · 直接引用 12` |
| `[b]` | `模板 B｜类型名 B（示意）· ROCK 0.55` |
| `[bl]` | `B 侧只列 ACTIVE 模板 —— 归档不列、不 fallback、不自动找相似` |
| `[n1]` | `候选交共用N2 预览／确认；提交只执行直接绑定 mutation` |
| `[n2]` | `不自动 pin；不为维持旧 Effective Value 生成 SET` |
| `label` | `态1｜选 A → 选 B（发起候选 · B 只列 ACTIVE）` |

### 态2｜预览分区：直接改绑对象 × 有效影响对象（复用共用N2）

- **验证的 interaction topology**：共享N2「Replace候选将“直接改绑对象”与“有效影响对象”分区标识」；N2 代表验收 3「Replace的继承child显示为有效影响对象，不能标成新增直接写入目标」；两集不能互代（汇编 §14）。
- **帧内控件／文案**：

| 节点 | 文案（逐字） |
|---|---|
| `[st]` | `候选变更（未确认）· 换绑 A → B · 分区标识` |
| `[rs]` | `改绑对象与有效影响对象分区；两集两数不能互代` |
| `[s1]` | `直接引用数 12` |
| `[s2]` | `Effective consumer 数 31` |
| `[s3]` | `最终结果变化数 24` |
| `[s4]` | `新增 Error·Warning 2E · 3W` |
| `[p1]` | `直接改绑对象（DirectReferenceSet(A) · 12）` |
| `[p1a]` | `Fish_A / Mature（Species 绑定）` |
| `[p1b]` | `SpeciesPreset_P（Preset 绑定 · 示意）` |
| `[p2]` | `有效影响对象（EffectiveConsumerSet · 31）` |
| `[p2a]` | `Fish_B / Mature（经物种层继承）` |
| `[p2b]` | `Fish_C / Young（经物种层继承）` |
| `[n1]` | `继承 child 只出现在有效影响分区 —— 不是新增直接写入目标` |
| `[n2]` | `after 统计来自全图 Resolve，不能只 diff 模板 payload` |
| `[n3]` | `正文与确认复用共用N2 组；本帧为 Replace 形的分区别注` |
| `label` | `态2｜预览分区：直接改绑 × 有效影响（复用共用N2）` |

- Policy 直接引用按汇编 §14 入 Direct 集（「Policy Template 的 direct refs 是 Species policy source binding 与 SpeciesPreset policy binding；Affinity 没有 `policySourceOverride`，不得虚构」）——本帧示例列 Species／Preset 两行；Policy 行在态3 `[n2]` 以条件句钉住（仅当 A 为 Policy 模板）。

### 态3｜提交后：patch 保留 · 无新增 pin

- **验证的 interaction topology**：卡11 Acceptance 逐字「Species 直接引用 A、child 继承它时，换绑只写 Species binding；child 经 Resolve 消费 B，仍无新增 sourceOverride」「已有 ADD／SET／CLEAR 保留；不为维持旧值制造 SET；after 统计来自全图 Resolve，不能只 diff 模板 payload」；Durable mutation「只写直接 source bindings；operations／patches 按所引边界保留」。
- **帧内控件／文案**：

| 节点 | 文案（逐字） |
|---|---|
| `[st]` | `提交完成 —— 只写了直接绑定` |
| `[rs]` | `operations／patches（ADD／SET／CLEAR）原样保留` |
| `[r1]` | `Fish_A / Mature（直接绑定）｜Source A → B · 本层 调整 −0.10 保留` |
| `[r2]` | `Fish_B / Mature（经继承）｜随物种层 A → B · 无新增 sourceOverride` |
| `[r3]` | `Fish_C / Young（经继承 · 已有 SET 0.50）｜SET 保留 · Effective 仍 0.50 —— 受遮罩 · 结果未变` |
| `[n1]` | `受遮罩未变 ≠ 无影响` |
| `[n2]` | `若 A 为 Policy 模板：其直接引用＝Species policy binding 与 SpeciesPreset policy binding（属本次写集）；Affinity 无 policySourceOverride，不虚构` |
| `[n3]` | `不自动 pin；不为维持旧值制造 SET；同值不改变拓扑边界` |
| `label` | `态3｜提交后：patch 保留 · 无新增 pin` |

---

## 2. annotation 计划

同共用N2 册范式；`#73` 双层链接；`⚑UNIMPL（GAP 号待补）` 逐帧。逐帧 payload：

**态1（proj_rr_1）**
```
⚑UNIMPL（GAP 号待补）：A→B只重写DirectReferenceSet(A)；B仅列ACTIVE，不fallback、不自动pin、不保值生成SET。
→ 规则：https://github.com/futouyiba/HitFish-Up/blob/main/docs/review/ui-component-contract-r2/contract-cards.md#replace-references
```

**态2（proj_rr_2）**
```
⚑UNIMPL（GAP 号待补）：DirectReferenceSet与EffectiveConsumerSet分区；继承child只进有效影响，不是新增直接写入目标。
→ 规则：https://github.com/futouyiba/HitFish-Up/blob/main/docs/review/ui-component-contract-r2/contract-cards.md#rebase-impact-preview
```

**态3（proj_rr_3）**
```
⚑UNIMPL（GAP 号待补）：只写直接source bindings；patch保留、无新增sourceOverride；after统计来自全图Resolve。
→ 规则：https://github.com/futouyiba/HitFish-Up/blob/main/docs/review/ui-component-contract-r2/contract-cards.md#replace-references
```

---

## 3. 与契约卡的映射（有卡必引；无卡必标待裁）

| 设计元素 | 契约条款 | 逐字依据（节选） |
|---|---|---|
| 态1 只重写 Direct 集 | 卡11 Actions／Durable mutation | 「提交只执行下述直接绑定 mutation」；「只写直接 source bindings」 |
| 态1 B 只列 ACTIVE | 汇编 §14 生命周期；卡9 Must not；汇编 §8 | 「归档后既有引用继续 Resolve／Publish，但不可新建引用」；「普通 Source Picker…隐藏／降级归档项」；「禁止任何自动 fallback」 |
| 态1 不自动 pin／不保值 SET | 卡11 Must not（沿 Must not「不为维持旧值制造 SET」）；汇编 §14 | 「Replace A→B 只重写…保留既有 operations／patches…禁止为保持旧 Effective Value 生成 `SET`」 |
| 态2 两分区标识 | 共享N2·共用显示 | 「Replace候选将“直接改绑对象”与“有效影响对象”分区标识」 |
| 态2 child 归有效影响分区 | 共享N2 代表验收3 | 「Replace的继承child显示为有效影响对象，不能标成新增直接写入目标」 |
| 态2 四项统计 | 汇编 §14 | 「影响面分列直接引用数／Effective consumers 数／最终结果变化数／新增 Error·Warning，四者可不同」＋「后两项归卡10／11等候选变更场景」（帧内 UI 标签取卡12 形「Effective consumer 数」，词形差异登记为 C2） |
| 态2 Policy 直引清单 | 汇编 §14 | 「Policy Template 的 direct refs 是 Species policy source binding 与 SpeciesPreset policy binding；Affinity 没有 `policySourceOverride`，不得虚构」 |
| 态3 只写 Species binding | 卡11 Acceptance | 「换绑只写 Species binding；child 经 Resolve 消费 B，仍无新增 sourceOverride」 |
| 态3 patch 保留 | 卡11 Acceptance／Durable mutation | 「已有 ADD／SET／CLEAR 保留」 |
| 态3 全图 Resolve | 卡11 Acceptance | 「after 统计来自全图 Resolve，不能只 diff 模板 payload」 |
| 态3 受遮罩未变 ≠ 无影响 | 汇编 §8 | 「Preview 至少区分…结果未变但被本层 SET／操作遮罩…**被遮罩 ≠ 无影响。**」（态3 为提交后当前态，遮罩类目沿 §8 分类词表；「结果未变」的 before/after 判读语境在卡10／11 —— 卡4 依据行同句） |
| **待裁／非契约项** | — | 示例数据（类型名 A/B、ROCK 0.40/0.55、Fish_A–C、SpeciesPreset_P、12/31/24/2E·3W、调整 −0.10、SET 0.50）全部为示意；帧尺寸为建议 |

---

## 4. 验收判据

**状态齐**：
1. 三态齐备（发起／预览分区／提交后）；组名含「（三态）」。
2. 态2 两分区标签各 ≥1 枚；四统计四节点分列。

**动作走通**：
3. 态1 → 态2 → 态3 的候选链在帧序可读；态2 `[n3]` 复用声明指向共用N2 组（跨组抽查：N2 组已在 `#62` 批落地；`#63` 批不得改动它——邻居判据）。
4. **态2 分区一（`[p1]` 及其子行）内「经继承」0 命中**——证「继承 child 不进直接改绑分区」。

**异常有落点**：
5. 态3 三类消费者行齐：直接绑定＋op 保留／经继承＋无新增 sourceOverride／已有 SET 保留·结果未变；`[n1]`「受遮罩未变 ≠ 无影响」句在。
6. 态3 `[n3]` 的三否定（不自动 pin／不保值 SET／同值不改拓扑）逐字在。
7. 态1 `[bl]` 归档约束句在。

**annotation／证据**：
8. 三帧 annotation 均为局部短形：默认不超过两行、一个稳定规则直链；必要状态保留；批次说明登记规则版本与必要取证覆盖范围；旧图取证版本单列；锚点命中、`&` 计数＝0 报原始数。
9. 保留整组回读、共用N2／卡10邻组未变、节点增量及新增宿主按区域枚举；删除「annotation 四段物理结构」作为验收谓词。

---

## 5. 冲突清单指针

见 [conflicts-pending](0.3.4.0-B-conflicts-pending.md)：C1（GPT 线卡5 组并存）、C2（统计词形变体）、C3（示例数值归属）、C4（EffectiveConsumerSet 统计口径）、C8（Replace 的 B 是否可为 Concrete——本稿按 §8 矩阵只列模板，未单列待裁而是按卡面推定，见冲突文件说明）。
